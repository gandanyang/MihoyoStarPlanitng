# 《归星物语》移动端人工测试方案 v1.0

> 目的：验证"移动端（触屏）可完成全部验收路径"（顶层设计 §4.1），并收集真实触屏体验问题。
> 配合使用：《试玩观察表-v0.5.2.md》（四感受观察）与本文档（触屏/布局/存档专项）。
> 状态：待执行（制作人先测，测试后据此决定是否做移动端 gameplay E2E）。

---

## 1. 测试目标（三个问题）

1. **触屏能不能玩**：虚拟摇杆 + 交互键可用、不误触、不连发
2. **界面能不能看**：HUD / 对话框 / 商店 / 背包在窄屏不遮挡、不溢出、不被刘海/手势条挡住
3. **进度稳不稳**：切后台、杀进程、刷新后不丢进度（pagehide 兜底实测）

## 2. 测试环境准备

- **手机**：任意智能手机（建议 ≥6 英寸），记录型号 / 系统 / 浏览器 / 屏幕尺寸
- **网络**：手机与电脑连**同一 Wi-Fi**
- **地址**：手机浏览器打开 `http://192.168.31.195:5173/`（dev server 已在运行）
  - 若打不开：Windows 防火墙可能拦截，需放行 5173 端口
  - IP 变了可重新 `ipconfig` 查 IPv4
- **建议**：无痕模式打开（避免旧存档干扰）；全程**横屏**（游戏是 800×600 横屏，Scale.FIT 等比缩放）
- **开始前**：无痕模式下首次进入即全新存档，无需手动清档

## 3. 测试流程（按顺序，每场景记录：通过 / 问题 / 截图）

| # | 场景 | 操作 | 重点验证 |
|---|------|------|---------|
| S1 | 标题 | 加载后看标题画面，点"开始/Enter" | 加载速度、标题布局、进入按钮可点 |
| S2 | 车站序章 | 读邮件对白 → 点跳过 → 用**摇杆**向右走 | 对白显示、Skip 可点、摇杆跟手 |
| S3 | 大门教程 | 靠近夏雅按交互键 → 背包 → 使用钥匙开门 | 交互键范围、背包打开/关闭 |
| S4 | 农场第一天 | 锄地×3 → 播种×3 → 浇水×3 | 面前格子高亮、操作反馈、教学提示 |
| S5 | 睡觉 | 走进木屋 → 床铺上/床边按 E → 教程完成 | **进屋路径可发现**、床边交互、获得斧头 |
| S6 | 第一章 | 小镇接任务 → 森林采集（对话）→ 交付 | 地图切换、NPC 交互、对话推进 |
| S7 | 商店/背包/每日任务 | 开商店买卖、开背包、看每日任务面板 | 窄屏布局、按钮可点、金币/钻石反馈 |
| S8 | 挖矿/砍树 | 矿洞挖矿、农场砍树 | 体力反馈、交互距离 |
| S9 | 观星收尾 | 晚上 ≥20:00 去观星点 → 对白 → **点三选项** → 结算 → 继续游玩 | **触屏点选项**、结算面板按钮 |
| S10 | 存档恢复 | 睡觉存档后：切后台 → 杀进程 → 重开；再刷新一次 | 进度恢复到哪、是否丢档 |
| S11 | 系统干扰 | 下拉通知、来电、Home 返回、横竖屏切换 | 回来后画面/进度正常 |

## 4. 重点观察项（怎么测、注意什么）

### 4.1 触屏交互（最重要）

- **摇杆**：拖动是否跟手？是否有"拖不动/飘"？误触率（手指放在屏幕上会不会误动角色）？
- **交互键（E 键替代）**：点击是否灵敏？会不会**连点触发两次**（比如对话一下跳过两句、商店开了又关）？
- **观星三选项**：触屏必须靠**点按钮**（手机没有 1/2/3 键）——选项按钮够不够大、好不好点？
- **对话推进**：点对话框推进、打字机跳过，是否顺手？
- 手机没有键盘，所有"按 [B]/[R]/[E]"提示在触屏上是否有对应按钮？如果缺，记录在哪一步卡住。

### 4.2 布局与视觉

- **刘海/挖孔屏**：顶部 HUD（时间/金币）是否被刘海挡住？右上角任务/每日任务面板是否被挖孔遮住？
- **底部手势条**：对话框底部是否被 iOS 小白条 / 安卓手势条挡住？
- **立绘**：对话头像（96×96）显示是否正常、有没有溢出/变形？
- **商店/背包**：按钮是否超出屏幕？文字是否被截断？要不要滚动？
- **横屏适配**：旋转手机后画面是否错位？

### 4.3 性能与音效

- 切图 / 挖矿 / 观星演出时是否卡顿、掉帧明显？
- 连续玩 20 分钟：发热 / 卡顿是否加剧？
- 音效：静音键是否生效、音量是否合适、首次交互后是否有声音（浏览器音频需用户手势解锁）？

### 4.4 存档（P0 重点）

- 睡觉存档后 → 切后台 → **杀掉 App/浏览器** → 重开：进度恢复到哪？（期望：回到最近位置，教程不重开）
- 玩到一半切后台 5 分钟再回来：不丢进度？
- 刷新页面：恢复到最近场景？
- 每次记录：**丢没丢进度、恢复到哪一步**

## 5. 判定标准

| 级别 | 定义 | 例子 |
|------|------|------|
| **P0 阻断** | 无法继续核心流程 | 教程睡不了觉、主线断链、黑屏、坏档、触屏无法操作核心功能 |
| **P1 问题** | 影响体验但可绕过 | 布局遮挡、误触率高、按钮太小、无触屏按钮导致卡操作 |
| **P2 建议** | 体验优化 | 摇杆手感、音效、视觉细节 |

## 6. 记录表（每场景一行）

| 场景 | 设备 | 结果(通过/P0/P1/P2) | 问题描述 | 截图 |
|------|------|---------------------|---------|------|
| S1 标题 | | | | |
| S2 序章 | | | | |
| S3 大门 | | | | |
| S4 农场 | | | | |
| S5 睡觉 | | | | |
| S6 第一章 | | | | |
| S7 商店背包 | | | | |
| S8 挖矿砍树 | | | | |
| S9 观星 | | | | |
| S10 存档 | | | | |
| S11 系统干扰 | | | | |

**设备信息**：型号 ______ 系统 ______ 浏览器 ______ 屏幕尺寸 ______ 横/竖屏 ______

## 7. 避坑清单（测试前必读）

1. **全程横屏**——竖屏下 800×600 游戏会非常小，属预期，不作为 bug 记录
2. **先清档**：用无痕模式，或先完整测完一遍再刷新（避免旧档干扰教程流程）
3. **记录设备与浏览器**：iOS Safari 对 localStorage / 全屏 / 音频限制和安卓 Chrome 不同，问题要标注浏览器
4. **别把"不习惯"当 bug**：摇杆手感属于主观反馈（P2）；真不能操作才算 P0/P1
5. **每步截图**（手机自带截图），问题截图比文字描述有用
6. **真机优先**：浏览器模拟器触控/性能与真机差异大，本方案以真机为准
7. 若某一步卡住，记下"卡在哪一步、提示是什么、我按了什么"，不用勉强走完——这本身就是最宝贵的 bug 信息

---

> 测试完成后：把记录表与 P0/P1 清单反馈给制作人；据此决定是否做移动端 gameplay E2E 与修复优先级。
