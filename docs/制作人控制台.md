# 制作人控制台

当前版本：
v0.6.x（序章 v0.7 + 第一章 v0.8 已落地）

当前施工：
主线剧情语音生成与接入（opencode_voice_main 执行中）

设计确认：
Codex ✅（语音管线/选角/台词/适配规范已定稿）

施工：
OpenCode ⏳（语音批量生成 + VoiceBank 接入）

验证：
Trae 等待（语音接入后）

禁止修改：
机器人
美术
宇宙线

---

## 已完成（2026-08-03~04 落档，勿重复排期）

| 任务 | 落档证据 |
|------|---------|
| 序章 v0.7 + 第一章 v0.8（含青禾镇） | `f00d7e6` 施工完成 + `312efbf` Trae 验证通过（tsc 0 错 + 14/14 探针全绿） |
| P1 交付后观星引导 | `cae2e3f` +1 行「夜晚的庄园…」+ 探针 8→9；`edcf6e6` 验收落档（24/24、14/14、25/25） |
| FEATURE-036 旧机器人 | `c731f87` 制作人验收通过（回归补跑探针见下） |
| 一键出售（背包+商店） | `6d575f3`（probe-sell-all 18/18 + 回归全绿） |
| 体验债务 E-07/E-08 | 代码已落地（StorySystem GATE/WATER 对白，制作人拍板文案） |
| 剧情线审核批 4 张卡（信息节奏/术语/NPC称呼/乡村生活感） | `aa45eb9` |
| 测试轮次 01 批 1 自动化回归 | `312efbf`（tsc 0 错 + 14/14 探针全绿） |
| 测试轮次 01 批 3 存档·浏览器侧预验证 | `590f249`（probe-save-restore 24/24） |

## 下一步队列（开工前先看这里，自助领单）

1. **主线剧情语音生成与接入**（施工中）：任务-主线剧情语音生成与接入.md；选角表/台词文本/适配分析已定稿；音高自检防漂移
2. **FEATURE-036 回归补跑**：tsc + probe-bug036-robot-acquire + probe-automation + test-tutorial 全绿（验收已过，补正式回归落档）
3. **返回标题逻辑修复**（Batch B）——方案 A：显式 save() + `scene.start('title')`，剥掉 `location.reload()` 的 `?reset=1` 清档风险（代码当前仍为 reload，未修）
4. **测试轮次 01 收尾**（Batch C）：批 2 美术目测 + 批 3 真机存档（制作人执行）；指引/清单已就绪（`fc5ad41`、`34f21b1`）
5. **测试后二选一**：Batch D 剩余美术（商店/地图/林澈 32×32）或 v0.7 关系记录系统 Phase 1（docs/design/NPC好感系统规划-v0.1.md）

> 队列维护：制作人/Codex 更新；当前施工中的在途文件（MapScene/StorySystem/StoryDialogue/src/audio）其他线避让。

---

## StorySystem 单写者协调机制（制作人拍板 2026-08-04）

> 冻结目标：**多人无协调并发修改 StorySystem 的状态**。剧情开发不冻结，照常推进。

1. **StorySystem.ts 为单写者文件**：同一时刻只允许一条线持有修改权，持有者登记在本表。
2. **当前持有**：无人（工作区内容与 HEAD 一致，仅行尾脏状态；`git diff` 为空）。
3. **申请流程**：开工前 `git status` 确认 StorySystem 干净 → 在控制台登记持有 → 改动小而频、即改即提交 → 完成后注销。
4. **剧情文本红线**：涉及台词/角色/世界观的关键改动，必须先经制作人/设计文档确认（剧情权限规则），执行方不得自行扩写。
5. **语音接入边界**：走 `src/audio/VoiceBank.ts` + `StoryDialogue.ts`（当前另一线在途），**不碰 StorySystem 台词文本**。
6. **当前在途登记**：`src/ui/StoryDialogue.ts` + `src/audio/`（语音接入，制作人协调的另一线持有）；`StorySystem.ts` 无人在途。
