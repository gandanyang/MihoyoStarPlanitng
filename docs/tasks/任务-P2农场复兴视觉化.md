# 任务卡：核心场景美术升级（P2 农场复兴视觉化 · 第二弹）

> 状态：✅ **已完成**（Trae 2026-08-07，probe-farm-life 19/19 + farm/gate 系列探针回归全绿 + tsc 0 错）
> 立项：制作人 2026-08-07（「先做开大门的那个场景的升级 简单升级一下 然后去做农场美术升级」→「A+B，P2 先行 (推荐)」）
> 一句话：农场从「荒废（杂草/破损/没人气）」随 FEATURE-037 worldRestore 两态切换装饰组 →「复兴（菜畦层次/工具齐整/树下花丛/碎石小路）」，零新素材、零决策依赖、纯代码 Overlay 叠加。
>
> M1（农场瓦片升级，5 区 + tileset 扩格）**挂起**，Q1-Q6 待制作人拍板后另行施工。

---

## 一、背景

农场是玩家主场景，但除 M1-3 花园 / FEATURE-037 老屋两处修复点外，地图整体缺乏「回应玩家建设」的视觉反馈。视觉升级方案 v0.10 §五 P2 提出：荒废农场 → 玩家建设 → 归星岛重新有生命。本卡落地该方向的「复兴状态切换装饰组」，与 FEATURE-037（worldRestore）联动，复用既有 `isRestored()` 两态模式（先例：`setupGardenRestore` / `setupOldHouseRestore`）。

---

## 二、现状（施工前复核）

- 既有氛围（零资源）：`setupFarmAmbience`（涟漪 3 点 / 花草摆动 6 处 / 暖色光斑）、`setupGardenRestore`（isRestored('garden')，cols 28-32 rows 4-7）、`setupOldHouseRestore`（isRestored('oldHouse')，木屋 cols 3-8 rows 18-23）。
- 红线常量：`FARM_AREA`（col 12-28, row 8-16 农田）、`FARM_TREE_POSITIONS`（28 棵树）、3 出口（顶→森林 / 右→小镇 / 左下→屋内）、水池（cols 31-33 rows 19-22）。
- 交互点（装饰须避开）：gardenXiya(33,6) / dawnXiya(33,4) / oldRobot(28,3) / farmShop(24,18) / grandpaNote(1,6) / 老屋 pos(11,20) / 花园 pos(30,5) / 出生点(20,18.75)。
- Walls 层碰撞仅 gid 3/4/9-13（gid 6 木墙 / 8 花丛不碰撞）。
- 复用模板：gate 弹（`setupGateDecorations` + `gateLife` 统计）、town 弹（小鸟 tween）、M1-3 两态模式（`stage`/`restored` + 装饰组销毁重建）。

---

## 三、实施内容

新增 `setupFarmDecorations()`（create() 中 `mapKey === 'farm'` 分支，置于 setupOldHouseRestore 之后），四组装饰两态切换，一次性 `isRestored()` 决定装饰组（不做运行时事件监听）：

### Task 1：菜园层次（驱动 `isRestored('garden')`）

- 荒废 4 组：破木桩(26,3) / 倒伏菜架(27,6) / 破陶罐(32,8) / 乱草堆(34,8)
- 复兴 5 组：菜畦色块×2((30,8)/(31,8)，半透明深浅绿) / 篱笆×2((26,4)/(27,7)) / 花簇(31,3)

### Task 2：工具区域（驱动 `isRestored('oldHouse')`）

- 荒废 4 组：断柄锄(1,17) / 锈镰刀(1,19) / 破木箱(9,17) / 干裂木桶(12,18)
- 复兴 5 组：工具架(1,17) / 木桶(1,19) / 柴堆(9,17) / 水桶(3,17) / 磨刀石(2,17)

### Task 3：树荫区域（驱动 `isRestored('oldHouse')`）

- 荒废 3 处：树下枯草圈 (3,7) / (14,21) / (37,6)
- 复兴 3 处：蘑菇圈(3,7) / 花丛(14,21) / 白花丛(37,6) + **小鸟 ×1**（(37,4) 树冠上方，depth 5 > 树 4，小跳 tween）

### Task 4：碎石小路（驱动 `isRestored('oldHouse')`）

- 荒废 4 段：门前往农田走廊 row 17 土路裂缝 (6,17)/(8,17)/(11,17)/(12,17)
- 复兴 6 点：碎石点 (6,17)/(7,17)/(8,17)/(10,17)/(11,17)/(12,17)

### 统计

`public farmLife = { ruin, revive, wildlife }`（纯统计无逻辑，探针读取）：
- 全未恢复：ruin=15 / revive=0 / wildlife=0
- 仅 garden：ruin=11 / revive=5 / wildlife=0
- 全恢复：ruin=0 / revive=19 / wildlife=1

### 坐标核对

35 个候选坐标全部经脚本（farm.json 解析 Ground/Walls）验证：草地（gid 1）+ 无碰撞（Walls 0）+ 避开树位/农田/3 出口/花园覆盖区/木屋区/水池/既有花草摆动/全部交互点周边。

---

## 四、涉及文件

- `src/scenes/MapScene.ts`：字段（`farmLife`）、create() farm 分支（`setupFarmDecorations`）、新增 9 个方法（setupFarmDecorations + 8 个 build/create 辅助）
- `tests/probes/probe-farm-life.mjs`（新增）：三态（新档/garden/全恢复）+ 夜间 + 既有系统状态一致 + 无页面错误 + 截图 ×3
- **不修改**：`public/assets/maps/farm.json`、`farm_tileset.png`、存档/剧情/碰撞/出口/交互点/NPC 站位

---

## 五、验收标准

- ✅ 新档农场：荒废装饰 15 组、无小鸟
- ✅ 仅 garden 恢复：菜园层次复兴 5 组、其余仍荒废 11 组
- ✅ 全恢复：复兴 19 组、小鸟 1 只、无荒废残留
- ✅ 夜间（20:00）装饰不消失（无时间依赖）
- ✅ 既有复兴系统状态一致（gardenRestore.stage / oldHouseRestore.restored 不破坏）
- ✅ 全部装饰避开交互点/树位/农田/出口/碰撞（脚本核对 35/35 通过）
- ✅ `tsc` 0 错 + probe-farm-life 19/19 + 回归（farm-visual 21/21、restore-037 41/41、garden-xiya 10/10、gate-visual 12/12、gate-skip 9/9、locked-tools 5/5、resident-board 25/25）
- ✅ 存档/剧情/碰撞/地图 JSON 零改动

---

## 六、不做（红线）

❌ 换 TileSet / 改 farm.json / 重做地图（M1 挂起待 Q1-Q6 拍板）
❌ 触碰农田 FARM_AREA / 树木砍伐 / 出口 / 交互点 / NPC 站位
❌ 新增玩法系统 / 新地图 / 大规模新素材
❌ 修改存档结构、剧情文本

---

## 七、给施工 AI 的一句话

> 在 farm 场景叠加纯视觉 Overlay：复用 `isRestored()` 两态模式，四组装饰（菜园层次→garden；工具区/树荫/碎石小路→oldHouse）荒废↔复兴切换，全部零资源 Graphics + 1 只小鸟（depth 5）。坐标已脚本核对（草地+无碰撞+避开全部交互点）；补 probe-farm-life 三态探针并回归 farm/gate 系列探针。M1 瓦片升级挂起，勿混入本卡。

---

## 八、已知的既有探针问题（非本卡引入，供后续清理）

- `probe-farm-restore.mjs` 断言存档 `save.farm.restore.garden`（M1-3 旧字段）。FEATURE-037 决策 5 后恢复态改存顶层 `worldRestore`（`farm.restore` 仅用于旧档迁移不回退），故该断言恒为 null。恢复态实际持久化正常（探针自身「重进后 stage=3」通过）。建议由 FEATURE-037 后续维护更新该探针断言。
