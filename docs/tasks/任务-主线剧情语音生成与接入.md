# 任务：主线剧情语音生成与接入

> 状态：✅ 制作人批准（2026-08-04）｜执行：OpenCode ｜验证：Trae
> 目标：按既定管线，把主线剧情（序章 + 第一章 + 观星夜）所有可语音化台词生成音频，并最小侵入接入游戏

## 一、必读依据

1. `docs/design/配音台词文本-序章第一章-v0.1.md` — 台词清单（场景/角色/语气）
2. `docs/design/配音选角表-v0.1.md` — 音色 id / 参考音频 / 生成参数（制作人已拍板）
3. `docs/design/台词语音化适配分析-v1.0.md` — 每句筛选标记与特殊处理（**含制作人拍板：第一段=林澈男声，HR 通知=男声电子音**）
4. `src/systems/StorySystem.ts` — 台词常量（接入点）
5. `src/ui/StoryDialogue.ts` — 对话 UI（播放接入点）

## 二、管线（已验证，必须遵守）

- VoxCPM 调用入口：`E:\BINGdown\VoxCPM\mwedm\python.exe -m voxcpm.cli`
  - **禁止使用 `E:\BINGdown\VoxCPM\mwedm\Scripts\voxcpm.exe`（shim 损坏，直接崩）**
- 参数：`--cfg-value 2.4`（超短句 ≤4 字用 2.6）`--inference-timesteps 16`（超短句 20）`--no-denoiser --local-files-only`
- 台词格式：**标点连排、不用换行**（避免长停顿）；超短句注意声线漂移
- 语速：夏雅 生成后 `ffmpeg -filter:a "atempo=1.1"`；林澈原速
- 参考音频/文本：见选角表（`--prompt-audio` + `--prompt-text`；参考试听文本可用 `npm run tts -- --preview <音色id>` 重新获取准确文本）
- 批处理模式可用：`--input <逐行txt> --output-dir <目录>`（产出 output_001.wav…，需自行映射改名）

## 三、音高自检（强制，防女声串入）

生成后必须测量中位 F0（16kHz wav 自相关，可参考本项目已验证的脚本思路）：

- 男声（林澈/村长/爷爷）：中位 F0 应 ∈ [70, 180] Hz；>180 判定漂移 → 重跑（最多 3 次）
- 女声（夏雅/神秘少女）：中位 F0 应 ∈ [170, 320] Hz；<170 判定漂移 → 重跑（最多 3 次）
- 每句记录：初测值 / 重跑次数 / 最终值；漂移 >3 次仍未达标 → 标记待人工处理，不硬出

## 四、产出与命名

- 目录：`public/audio/voice/<角色>/<场景>_<序号>.wav`
- 角色目录：`linche` / `xiya` / `elder`（村长）/ `grandpa`（爷爷信·笔记）/ `girl`（神秘少女，可占位跳过）
- 场景前缀与台词文档一致：station / xiya / gate / dawn / harvest / evening / sow / water / town / elder / shard / forest / woodcut / mine / garden / robot / ending / branchA/B/C / finale
- 交付一份映射清单（`docs/design/语音文件映射清单.md`）：文件路径 → 台词文档编号 → 台词原文 → 生成参数（cfg/steps/atempo/F0 终值）

## 五、接入游戏（最小侵入）

- **不改剧情文本 / 不改 StorySystem.ts 台词内容**（AGENTS.md 红线）
- 建议：新增 `src/audio/VoiceBank.ts`：lineId（或 speaker+序号）→ 音频 key 映射；`StoryDialogue.ts` 在显示对白时按映射播放；**找不到音频则静默跳过，不阻塞对话**
- 内心独白（`inner: true`）播放时建议轻混响区分；做不到先原音播放并标 TODO
- 触发重放/切场景不重播（每行只播一次）

## 六、范围

- 配音：✅ 可直接语音化 + ⚠️ 内心独白（林澈）+ ⚠️ 特殊音色（爷爷信/笔记、神秘少女、HR 男声电子音）
- 跳过：❌ 旁白/舞台指示、❌ 系统/UI 提示、❌ 选项菜单
- 音色：林澈=清澈青年男声、夏雅=治愈少女声（1.1x）已定；爷爷用候选 A（`3a4e8378...`，参考已下载）暂配，村长同方向，神秘少女待定（可跳过或空灵女声占位）
- HR 手机通知：男声 + 电话感 EQ（ffmpeg 低通/窄带模拟），无现成音色就先用林澈声线+处理并标注

## 七、验收

1. 全部目标台词生成成功且音高自检通过（漂移清单透明）
2. 游戏内对话播放验证（`tsc` + 相关探针；按 TEST_RULES Level 2）
3. 更新 `docs/design/配音选角表-v0.1.md` 状态与映射清单
4. 提交说明：生成数量 / 漂移重跑统计 / 接入点 / 待制作人确认项（爷爷·村长的终审、神秘少女）
