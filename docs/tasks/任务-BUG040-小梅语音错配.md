# 任务卡：BUG-040 花匠小梅语音内容错误（P0）

> 登记：制作人 2026-08-04 真机反馈｜级别：**P0**（语音内容错配，严重出戏）｜状态：📋 待施工

## 症状

花匠小梅台词「只要用心，土地就会用丰收回报你。你的庄园也会一样的。」播放的语音内容是**「顺我者昌逆我者亡」**（串词/错配）。

## 已核实（Codex）

- 文本正确：`NPCSystem.ts:217`「只要用心，土地就会用丰收回报你。你的庄园也会一样的。」
- 映射正确：`voicebank.data.ts` garden_04 → 花匠小梅 + 上述文本
- **问题在音频文件本身**：`public/audio/voice/gardener/garden_04.wav` 内容错（生成时串词）

## 修复

1. **重录 garden_04.wav**：按正确文本「只要用心，土地就会用丰收回报你。你的庄园也会一样的。」用花匠小梅声线生成（本地 VoxCPM + F0 自检；参考声线找生成小梅配音批时用的那条，可从 tools/gen_mainline_voice.py 的 ROLES 配置或 voice_gen_log 确认）
2. **抽查小梅其余 6 条**（garden_01~03/05~07）：确认没有其他串词
3. 建议：批量配音完成后加一道"文本↔音频内容"抽查（听感或转写校验），防此类 P0 再犯

## 验证

- 游戏内小梅这句语音 = 文本；其余 6 条抽查无错配
- probe-voice 回归；tsc 0 错

## 红线

- 只重录语音，不改台词文本；探针同步；小步提交
