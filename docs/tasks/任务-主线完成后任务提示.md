# 任务卡：主线完成后增加下一步任务提示（需求）

> 登记：制作人 2026-08-04｜级别：P1（Demo 收尾引导，观星结局可达性）｜状态：✅ 已完成（b54c6ff）

## 需求

主线任务（星之碎片交付，SHARD_DELIVER）完成后，**HUD 目标区 / 任务面板没有下一步提示**——玩家不知道 Demo 结尾（观星夜）怎么触发。

现状：

- HUD 目标走 `getQuestObjective()`（`DailyQuestSystem`），主线完成后无后续目标文案
- 观星点仅"主线完成 + 夜晚 20:00 后"在 farm 右下出现（`MapScene` updateStargaze），无显式引导
- N-1 已在对话层加了村长台词+系统提示（待施工），但 **HUD/任务面板层仍缺失**

## 要求

1. 主线完成后，HUD 目标区显示下一步引导，建议文案（与 N-1 一致，避免两套说法）：
   - 「夜晚去农田边那块空地看看」/ 夜间可加「（20:00 后）」
2. 任务面板同步显示；主线完成前不显示（避免提前剧透）
3. 与 N-1 施工包协调：文案一致，N-1 落地后再接线，或同批落地

## 验证

- 交付碎片后 HUD 出现引导；观星完成后引导消失
- 白天/夜晚文案正确；tsc + probe-voice / quest 探针回归

## 红线

- 不改剧情文本（HUD 引导属 UI 提示层，走 L1）；不新增存档字段；探针同步
