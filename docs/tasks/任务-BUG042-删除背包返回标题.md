# 任务卡：BUG-042 删除背包页"返回标题"按钮

> 登记：制作人 2026-08-04｜级别：P1（入口重复，UI 冗余）｜状态：✅ 已完成

## 问题

B2 返回标题入口已迁移到 ESC/移动端菜单（MapScene.ts:625 显式 save + scene.start('title')；AndroidBackHandler.ts:85 回到主菜单），但**背包页的旧"返回标题"按钮忘了删**（BackpackPanel.ts:77 return-title 按钮 + 处理器）。

## 施工

1. 删除 BackpackPanel.ts 中 return-title 按钮（HTML）+ 对应事件分支
2. 若 onReturnTitle 回调因此无人使用，一并清理传参链（MapScene 传入处）
3. 保留新入口：ESC 菜单（桌面）/ 移动端菜单 / AndroidBackHandler 回到主菜单

## 验证

- 背包页无"返回标题"按钮；ESC/移动端菜单返回标题正常（存档保留）
- tsc + 相关探针回归

## 红线

- 只删背包按钮，不动新入口；不动存档；探针同步；小步提交
