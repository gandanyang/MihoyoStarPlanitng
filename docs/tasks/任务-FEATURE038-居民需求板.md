# 任务卡：FEATURE-038 居民需求板（林澈=连接者，v0.10）

> 状态：📋 待施工（P1）
> 立项：制作人 2026-08-06 拍板方向（程序员=连接者）｜ 方案：[林澈程序员身份-归星助手-v0.1.md](design/林澈程序员身份-归星助手-v0.1.md)

---

## 一、目标

林澈用"连接信息"的能力把分散的人与资源连起来：需求板显示村民需要什么，玩家交付后岛屿生活感增强。

## 二、实施内容

### 1. 需求数据

新增模块（建议 `src/systems/ResidentRequestSystem.ts`，模块级单例）：

```ts
interface ResidentRequest {
  id: string;
  npcId: string;
  npcName: string;
  item: string;       // 物品 ID（wood / 食物等）
  itemName: string;
  count: number;
  done: boolean;
  rewardDialogue: string; // 交付后 NPC 反馈（文案制作人定稿，不扩写）
}
```

示例（数值施工时平衡）：

| NPC | 需求 | 数量 |
|---|---|---|
| 小梅（花匠） | 木材 | ×10 |
| 老张（矿工） | 食物 | ×5 |

### 2. 需求板 UI

- 入口：村庄或农场的"信息板"交互物（或轻量 UI 面板）
- 显示：NPC 名字 + 需求 + 数量；已完成打勾
- 复用现有交互/面板模式，不新建重型 UI 系统

### 3. 交付交互

- 找对应 NPC 交付（或需求板一键交付，施工时按体验定）
- 背包有资源 → 扣除 → 标记 done → NPC 反馈对白 → 触发归星记录事件标签 `help_resident`
- 资源不足 → 提示缺什么

### 4. 存档

- 需求完成状态持久化（可选字段兼容旧档，参照 `farm.restore` 模式，版本号不递增）

## 三、不做（红线）

❌ 不塞进 QuestSystem 主线状态机（独立于星之碎片主线）
❌ 不做 NPC 需求动态生成 / 经济模拟（固定脚本需求）
❌ 不做旧电脑 / 农业记录工具（阶段 1，后置）
❌ 不新建"归星助手"大系统（归星记录 GuiXingRecordSystem 已存在，复用）

## 四、验收标准

- ✅ tsc 0 错
- ✅ 需求板显示 2 个 NPC 需求，状态正确
- ✅ 交付扣除资源、NPC 反馈对白、归星记录 help_resident 标签触发
- ✅ 资源不足提示明确
- ✅ 完成状态持久化、旧档兼容
- ✅ 不影响现有 NPC 对话 / 日程（回归）

## 五、关联

- GuiXingRecordSystem（归星记录）：help_resident 标签已存在，直接挂
- FEATURE-037 复兴循环：需求板可作为建设点/资源的轻量消费出口之一（可选联动，不阻塞）
- 林澈叙事：连接者方向（forest 系统思维台词已有，本功能是其玩法化）
