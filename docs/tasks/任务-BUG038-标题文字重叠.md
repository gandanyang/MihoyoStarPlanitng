# 任务卡：BUG-038 标题画面文字重叠

> 登记：制作人 2026-08-04｜级别：P1（存档玩家必现，影响标题第一印象）｜状态：📋 待施工线认领

## 问题

标题画面存在存档时（`hasSave()` 为真），提示「检测到存档，将自动继续游戏」与副标「星黎庄园的归乡之时」重叠——两者坐标均为 `y=300`（`src/scenes/TitleScene.ts`）。

## 修复建议（二选一）

1. **存档提示下移**：`y=300` → `y≈340`（避开副标，且不与 y=380 的开始提示重叠）
2. **有存档时副标上移或隐藏**：副标 y=300 → y=270，或 hasSave 时不显示副标

推荐方案 1（改动最小，不影响无存档玩家的副标淡入演出）。

## 验证

- tsc 0 错
- 有存档/无存档两种状态各看一次标题画面，确认无重叠（浏览器探针可选：`probe-title-avatar` 回归）

## 红线

- 只动 TitleScene 布局坐标，不改剧情/存档/Logo 资源
