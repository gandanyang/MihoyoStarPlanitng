# 任务-移动端问题优先级排序-v0.5.2（Mobile Polish）

> 状态：**已登记**（2026-08-02，制作人输入）。
> 用途：移动端当前问题优先级排序 + v0.5.2 Mobile Polish 开发顺序。
> 执行前提：P0 完成后按三阶段推进；本计划不含新系统/新玩法，遵循 AGENTS.md（稳定 > 新功能）。

---

## 当前移动端问题优先级排序

### P0：第一次睡觉任务 Bug（必须先修）

**级别：最高优先级。**

**原因**：第一次睡觉时——

```
序章
 ↓
农场教学
 ↓
第一天结束
 ↓
第二天开始
```

它是玩家第一次完整体验"时间循环"的节点。如果这里坏，玩家感受不是"一个小 bug"，而是"这个游戏世界的规则不可信"。

**建议重点检查：`StorySystem`**

第一次睡觉应该是：

```
sleep()
 ↓
advanceDay()
 ↓
恢复体力
 ↓
作物成长
 ↓
触发第二天剧情
 ↓
更新任务状态
 ↓
保存
```

**检查点 1：睡觉事件执行顺序问题**

例如：`sleep() → 保存 → 剧情推进 → 任务更新` 会导致**保存的是旧状态**。

正确顺序：

```
sleep()
 ↓
全部状态更新
 ↓
save()
```

**检查点 2：新手任务状态没有闭环**

例如任务"第一次睡觉"：
- 睡完前：`status: active`
- 睡完后：应该 `completed`
- 但可能变成 `collected` 或者仍然 `active`

**执行要求**：这个建议让 AI 单独做，**不要顺便改 UI**。任务系统属于高风险文件。

### P1：任务列表 UI 不合理

**这个比 Bug 更重要**——任务系统是玩家每天看的东西。目前设计大概率还是"开发者视角 UI"，不是"玩家视角 UI"。

**移动端任务列表应避免**：大量文字堆叠。

**建议改成：当前任务卡片**

```
┌─────────────┐
  星光初遇
─────────────
  帮夏雅修复庄园

  ✓ 收集木材 5/5
  ○ 修复房屋

 奖励:
  50金币
└─────────────┘
```

**不要展示所有历史任务**——手机屏幕很小。

**建议三个区域**：
- 今日任务
- 主线任务
- 隐藏线索

### P1：右下角 UI 过大

**典型移动端问题**。现在应该是 PC 按钮直接缩放，但手机不是这么玩的。右下角可能锄头/水壶/种子/互动/背包全部铺开 → 占据游戏画面，玩家感觉不是在玩庄园，是在操作按钮。

**建议：采用二游移动端方式**

常驻只留：
- 攻击/互动
- 背包
- 菜单

工具收起来。例如点击背包后出现锄头/水壶/种子/斧头/镐子：

```
平时： [背包]

点击：
[锄][水][种][斧][镐]
```

### P2：UI 整体升级

已进入 v0.5.2 范围——玩法达到一定程度后，像素临时 UI 开始拖后腿。**建议不要全面重做**，先升级第一批：

1. **HUD**（优先级第三）
   - 现在：类似 `Day 1 10:00 金币100`
   - 升级：`☀ 第一天 / 10:00 AM / 💰100`，加入半透明背景

2. **对话框**（优先级第二）
   - 二游感主要来自这里
   - 建议：角色头像 + 名字 + 文本框，哪怕头像先用占位，效果提升巨大

3. **任务 UI**（优先级最高）

### P2：可能存在其他 Bug

手机测试应增加**异常行为测试**，不要只测正常流程：

**操作类**
- 快速连续点击
- 点击空地
- 点击 UI 边缘
- 打开菜单马上关闭
- 切地图过程中点击

**状态类**
- 睡觉中断
- 刷新页面
- 返回浏览器
- 重新打开

**存档类**
- 完成任务 → 退出 → 重新进入
- 检查：任务 / 作物 / NPC 关系 / 时间

---

## 下一阶段开发顺序调整

**不要继续开发新功能**，改成：

### v0.5.2 Mobile Polish

**第一阶段（稳定）**
- 修第一次睡觉 Bug
- 跑完整剧情测试
- 存档检查

**第二阶段（体验）**
- 重做任务 UI
- 缩小右下角 UI
- 手机点击反馈优化

**第三阶段（表现）**
- HUD 升级
- 对话框升级
- 氛围增强
