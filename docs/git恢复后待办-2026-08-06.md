# git 仓库恢复后待办清单（2026-08-06 21:20）

> 背景：git 对象库损坏 + 多 AI 会话并发操作，codex 正在恢复。
> 本清单记录恢复后需要处理的全部事项，按优先级排序。

---

## 一、提交相簿系统（FEATURE-041，代码已就绪且探针 10/10 全绿）

### 1.1 独立文件（可直接提交，无冲突）

| 文件 | 说明 |
|---|---|
| `src/data/PhotoAlbum.ts` | 相簿数据层（新增，84 行） |
| `src/ui/PhotoAlbumPanel.ts` | 相簿面板（新增，140 行） |
| `tests/probes/probe-photo-album.mjs` | 验收探针（新增，143 行） |
| `public/assets/photos/album/summer_garden.webp` | 1280×720 webp |
| `public/assets/photos/album/old_mine.webp` | 1280×720 webp |
| `public/assets/photos/album/hillside_view.webp` | 1280×720 webp |
| `docs/design/归星录-相簿系统设计-v0.1.md` | 设计文档（新增） |
| `docs/制作人控制台.md` | 已加 FEATURE-041 条目 |

### 1.2 存档字段（经查纯为相簿代码，可整体提交）

- `src/systems/SaveSystem.ts`：+6 行（album 字段 import/save/load），**无他人改动混入**

### 1.3 MapScene.ts（需拆分，与支线试点交织）

我的改动（5 处，约 20 行）：
1. import `PhotoAlbumPanel` + `unlockPhoto`
2. 声明 `photoAlbumPanel` 实例
3. HUD `album-btn`（约 668 行起）
4. `openPhotoAlbum()` 方法（5640 行附近）
5. update 冻结段（938 行）+ handleBackButton 层级（5667 行）
6. 3 个解锁触发点（花园恢复 / markMined / 老树 RECORD-01）

他人（支线试点）改动（同文件）：
- `sideXiyaGardenAsked/Done`、`sideElderTeaAsked/Done` flags + 事件逻辑
- import `XIYA_GARDEN_FLASHBACK`、`ELDER_STAR_FLASHBACK`、藤架/茶对话
- **且支线还改了 StorySystem.ts（冻结区！）**——提交前必须确认该改动归属与完整性

**拆分策略**：
- 若支线试点是他人**完整已实现** → 与相簿分开两个 commit，各自说明
- 若支线是**半成品** → 不能提交，MapScene 相簿接线也暂缓，等支线线收口
- 若无法安全拆分 → 与负责人协调后一起提交

### 1.4 提交信息建议

```
feat(FEATURE-041): 归星录·相簿 v0.1 — HUD「📖 归星录」入口 + 相簿面板（标题/描述/获得方式 + webp 图）

- PhotoAlbum 数据层（PHOTO_DATABASE 3 张 + unlockPhoto 幂等 + 存档 album 可选字段）
- PhotoAlbumPanel（半透明覆盖层 z220，未解锁剪影占位，复用 panelFadeIn/Out）
- MapScene 接线：HUD 按钮 + 冻结 + 返回键层级 + 3 解锁点（整理花园/矿洞探险/后山老树）
- 图片资源：3 张 1280×720 webp（ComfyUI 图转码）
- probe-photo-album 10/10；回归 dialogue-history 13/13、farm-restore 25/25
```

---

## 二、修复相簿遗留问题（代码排查发现）

### 2.1 `handleFarmTap` 缺相簿守卫（移动端穿透）

- 位置：`src/scenes/MapScene.ts` ~5280 行 `handleFarmTap`
- 问题：移动端打开相簿时，点击农田可能穿透执行种田逻辑（update 层已冻结，但这是独立 pointer 入口）
- 修复：在 `if (this.endingPanel?.isOpen()) return;` 旁加 `if (this.photoAlbumPanel?.isOpen()) return;`

### 2.2 存档字段位置说明

- `album` 放 SaveData **顶层**（非 player/world/farm）——语义是"归星录收藏"，独立于玩家状态
- 旧档兼容：`data.album ?? []`，无迁移成本

---

## 三、git 历史损坏事项（codex 处理中，恢复后确认）

- [ ] 确认 `main` ref 恢复指向正确 commit
- [ ] 确认 `da9a6e6`（round2 宣发图）提交完好
- [ ] 确认 FEATURE-040（`81b0eeb`）提交对象恢复
- [ ] 对方 3 个提交（视觉方案/支线方案/青禾镇生活化）工作区文件是否保留
- [ ] `git fsck --full` 复查 broken link 数量（历史孤儿对象 vs 新损坏）
- [ ] 评估是否值得做 `git gc --prune=now` 清理孤儿对象（有风险，稳定优先）

---

## 四、当前工作区他人改动（恢复后需确认归属，勿误提交）

- `src/systems/StorySystem.ts`（支线试点对白，**冻结区**）
- `src/data/MemoryFlashbacks.ts`（XIYA_GARDEN/ELDER_STAR 闪回）
- `src/ui/MemoryFlashback.ts`（+11）
- `src/audio/voicebank.data.ts` + `tools/gen_mainline_voice.py`（语音线）
- `tests/probes/probe-esc-menu.mjs` 重写 + 2 个 esc tmp 删除（Esc 菜单线）

## 五、临时文件（恢复后删除，勿提交）

- `tools/_fix_missing_blobs.mjs`——我写的 git blob 修复脚本（21:10 创建，批量 hash-object 尝试，未完成即停手）
- 若 codex 恢复过程中有遗留 tmp 文件，一并清理
