# 归星物语 — Alpha 阶段开发指南

> 本项目正进入 **Alpha 稳定阶段**。所有 AI 开发必须遵守本指南。
>
> **核心原则：稳定 > 新功能，体验 > 系统数量，完成一个闭环 > 添加十个半成品。**

## 当前阶段目标

制作一个 **15~30 分钟完整体验 Demo**。

## 优先级

### P0（最高）稳定性

- 修复 bug
- 防止黑屏
- 防止存档损坏
- 防止场景切换异常
- 保证教程流程稳定

### P1 提升玩家体验

- UI 反馈
- 对话体验
- 音效反馈
- 操作手感
- 新手引导

### P2 内容增强

- NPC 剧情
- 小任务
- 美术替换

## 禁止事项（非常重要）

未经允许，**不要**：

- ❌ 新增战斗系统
- ❌ 新增抽卡系统
- ❌ 新增大地图
- ❌ 新增复杂养成
- ❌ 重构整个架构
- ❌ 替换 Phaser 技术方案
- ❌ 引入后端
- ❌ 大规模修改已有系统

## 架构问题处理

如果发现架构问题：**先提出方案，不直接重构。**

## 修改代码规则

每次修改前：

1. 先阅读相关文件
2. 理解现有数据流
3. 找到最小修改点

不要"为了方便"创建重复系统。例如：

- 已有 `Inventory.ts` → 不要创建 `NewInventory.ts`
- 已有 `StorySystem.ts` → 不要创建 `DialogueManager2.ts`

## 输出要求

### 每次执行任务前

先说明：

- 你理解的问题是什么
- 准备修改哪些文件
- 为什么这样修改

### 修改完成后

说明：

- 修改内容
- 是否影响存档
- 是否需要测试
- 测试结果

## 测试要求

修改完成后优先运行：

1. TypeScript 类型检查
2. Vite build
3. tutorial 流程测试

> 不要只说"应该没问题"，**必须验证**。

## 开发原则

- 稳定 > 新功能
- 体验 > 系统数量
- 完成一个闭环 > 添加十个半成品

请把《归星物语》当作一个正在进入 Alpha 阶段的独立游戏项目维护。

## 剧情权限规则

涉及以下内容时：

- 主角成长
- 角色身份
- 世界观真相
- 关键剧情节点
- 玩家选择

执行 Agent 不得自行扩写或改变。

必须遵循：
顶层设计.md > 剧情规划文档 > 实现需求。

## Design Authority

当实现、规划文档、Agent建议冲突时：

优先级：

1. 制作人最终决策
2. 顶层设计.md 当前版本
3. 功能规划文档
4. Agent自主建议

Agent不得自行恢复已废弃字段。