# 问题追踪（Bugfix 汇总）

> 用途：集中登记人工测试 / 自动化测试发现的全部问题，跟踪状态与修复，避免遗漏与重复排查。
> 维护：每个 bug 修复后在此更新状态与修复 commit；复测结论同步登记。
> 级别定义：P0 = 阻断核心流程 / 坏档 / 黑屏；P1 = 影响体验可绕过；P2 = 体验优化建议。

---

## 问题清单

| ID | 标题 | 级别 | 状态 | 修复 | 复测 |
|----|------|------|------|------|------|
| BUG-001 | 回到床上按 E 无法完成教程（农场旧睡点与床的实际位置脱节） | P0 | ✅ 已修复 | 屋内真实床铺判定（`e99daeb`）+ 农场木屋地板衔接（`95255ea`）+ 睡觉判定放宽为相邻 1 格（本轮） | ⏳ 待移动端复测 |
| BUG-002 | 教程中提前睡觉永久卡死（浇水前睡觉 → 次日作物已熟/无种子） | P0 | ✅ 已修复 | 教程中未到 evening_talk 时按交互不跨天并提示（本轮 MapScene 守卫） | ⏳ 待复测 |
| BUG-003 | 移动端没有背包按钮（教程无法使用钥匙开门） | P0 | ✅ 已修复 | TouchControls 新增「背包」按钮（`95255ea` 并入） | ⏳ 待复测 |
| BUG-004 | 横屏看不到背包按钮（`innerWidth>=800` 误判桌面） | P1 | ✅ 已修复 | 改为触屏能力判断（`979c375`） | ✅ probe 9/9 |
| BUG-005 | 车站跳过开场按钮在剧情对话结束后未隐藏 | P2 | ✅ 已修复 | 对话完成自动移除（`95255ea` 并入） | ✅ probe |
| BUG-006 | 引导任务（挖矿/砍树）在教程期间提前投放 | P1 | ✅ 已修复 | 教程完成才注入（`injectGuideQuests`） | ✅ 探针 |
| BUG-007 | 移动端竖屏画布过小（375×281）+ DOM UI 与画布错位 | P1 | ⏳ 方案已拍板（方案 A），**实现延后——先移动端真机测试** | 见《任务-移动端竖屏适配.md》§4（制作人 2026-08-02 拍板方案 A） | — |
| BUG-008 | 存档丢失风险（移动端 `beforeunload` 不可靠） | P0 | ✅ 已加固 | `pagehide` 兜底 + 里程碑保存 + `apply()` 边界保护（`26e015f`） | ⏳ 待真机复测 |
| BUG-009 | 手机无法访问 dev server（Vite 仅监听 127.0.0.1） | P0 | ✅ 已修复 | `vite.config.ts` `host: true` | ✅ 局域网实测 200 |
| BUG-010 | 教程期无斧头时，树旁按交互被"需要斧头才能砍树！"吞掉 | P2 | ✅ 已修复 | `tryChopTree` 无斧头 `return false` 不吞交互（`77af4ce`） | ✅ 探针 |
| BUG-011 | 移动端操作提示文案未适配（显示"[E]/[B]/[R] 键"，触屏无键盘） | P1 | ✅ 已修复 | 4 文件提示句改 `isMobileLayout()` 双文案（标题/车站/钥匙/锄地/播种/浇水/商店/矿脉/每日任务）（`35cbac4`） | ✅ probe-mobile-text 6/6 |
| BUG-011 | 新玩家初始背包包含测试用 old_axe（降低工具解锁反馈） | P2 | ✅ 已修复 | `Inventory.ts` 默认值 `old_axe: 1` → `0`；教程睡觉才赠送（`tryTutorialSleep`），兼容老存档（`MapScene:370-371` 兜底 + `restoreAllInventory` 按存档恢复） | ✅ |
| BUG-012 | 第一次睡觉任务状态未闭环（每日任务跨天刷新丢失已完成未领奖任务） | P0 | ✅ 已修复 | 根因：`refreshDailyQuests()` 跨天仅保留引导任务，已完成未领奖任务被静默替换；修复：保留条件扩展为「引导任务或已完成且未领奖」（`DailyQuestSystem.ts` 1行） | ✅ probe-bug012-dailyquest |
| BUG-013 | 背包按钮无防抖，移动端可能双击发 | P1 | ✅ 已修复 | `TouchControls.ts` `pressBackpack` 新增 300ms 防抖 | ✅ |
| BUG-014 | sleeping 标志缺乏异常保护，抛错后永久阻塞睡觉 | P1 | ✅ 已修复 | `trySleep()` / `tryTutorialSleep()` 加 `try/finally` 保护 | ✅ |
| BUG-015 | 种子选择器打开时 handleFarmTap 未检查，可能误触农田 | P2 | ✅ 已修复 | `handleFarmTap` 新增 `if (this.seedSelectorEl) return` | ✅ |
| BUG-016 | 横屏手机仍显示 PC 键盘文案（[E]/[B]/[R]，`innerWidth>=800` 误判桌面） | P1 | ✅ 已修复 | `isMobileLayout()` 加入触屏能力判断：`innerWidth<800 \|\| isTouchDevice()`（`config.ts`） | ✅ probe-fix-mobile-hint 5/5 |
| BUG-017 | 教程提示条在场景切换后残留（farm 进屋睡觉后"回到屋内床前点交互睡觉"不消失） | P1 | ✅ 已修复 | 根因：`tutorialHint` DOM 挂 `document.body`，farm→house 切换无清理；修复：MapScene `create()` 注册 `SHUTDOWN` 事件 → `cleanupSceneDom()`（移除提示条/种子选择器） | ✅ probe-fix-mobile-hint 5/5 |
| BUG-018 | 矿洞 s1/i1 矿石生成在石簇碰撞区内，玩家走不进去无法采集 | P1 | ✅ 已修复 | `MineState.ts` s1(6,5)→(7,5)、i1(24,14)→(26,13)，移出石簇（s1 左上石簇 4-6,4-6；i1 右下石簇 23-25,13-15） | ✅ probe-layout 12/12 |
| BUG-019 | 移动端体验不到砍树/挖矿引导剧情（任务流程问题） | P1 | ✅ 已修复 | 根因①：爷爷笔记(1,3)距第一棵树仅17.9px + 交互基准误用label坐标(-8px偏移)，抢占砍树引导；根因②：挖矿引导在矿洞任意位置按交互即弹，未靠近矿脉。修复：爷爷笔记移(1,6) + 交互基准改椭圆坐标；挖矿引导改为矿脉旁24px才触发；砍树/挖矿引导扩为夏雅参与的7句剧情对话 | ✅ probe-guide-dialogue 6/6 + probe-note-vs-woodcut 2/2 |
| BUG-020 | 对话残留跨场景传递，新场景按交互被对话拦截 | P1 | ✅ 已修复 | 根因：Phaser 场景 start 复用实例，`if (!this.storyDialogue)` 复用残留实例（display:block 未清理）；探针 skipDialogue 次数不足触发。修复：StoryDialogue 新增公开 `reset()`（静默关闭不触发 onComplete），MapScene `cleanupSceneDom`（SHUTDOWN）调用 | ✅ probe-density-v053 9/9 + 回归全绿 |
| BUG-021 | 开场剧情被跳过：进游戏未进第二场景（开门场景），直接跳到后续，中间剧情丢失 | P0 | ✅ 已修复 | 根因：物理返回键在剧情对话中触发 `skip()` 跳整段对话 + 场景回退表 `gate→farm` 传送（教程未完成即跳关）；修复：返回键对话中只消费不跳过；场景回退仅限 `isTutorialDone()` 后（`7971122`） | ✅ 回归全绿 |
| BUG-022 | 任务卡进度被卡掉后不让睡觉：玩家被锁死无法跨天推进 | P0 | ✅ 已修复 | 与 BUG-021 连锁（返回键跳过剧情 → storyStep 卡在门阶段 → 睡觉守卫拒绝）；修复：返回键防传送 + 坏档自愈（`scene=farm` 但 storyStep 在门阶段 → 恢复时推进到 `clear_land` 并补锄头，StationScene `7971122`） | ✅ 坏档自愈探针 |
| BUG-023 | 跳过开场按钮没功能（点击无响应） | P1 | ✅ 已修复 | 根因：仅绑定 `click`，Android WebView 偶发不触发；修复：`pointerdown` 立即响应 + `click` 兜底（StoryDialogue Skip / StationScene 跳过开场，`7971122`） | ✅ |
| BUG-024 | 没有删档按钮/方式：玩家无法重置存档体验新流程 | P1 | ✅ 已修复 | 标题界面「清除存档」按钮居中显示 + 二次点击确认防误删（原按钮藏右下角难发现；`7971122`） | ✅ probe-clear-save 6/6 |
| BUG-025 | 横屏可走到屏幕左边（物理边界漏洞，玩家出界） | P1 | ✅ 已修复 | 根因：FIT 横屏画布居中产生两侧黑边，DOM UI（摇杆/按钮/HUD）按全屏容器定位偏移到黑边区；修复：`game-container` 尺寸=画布显示尺寸（`displaySize`）+ `position:relative`；TouchControls 改挂画布容器（`7971122`） | ✅ 回归全绿 |
| BUG-026 | 种植体验差（操作手感 1/5）：触控反馈/节奏/精度问题 | P1 | 🔴 专项进行中（P0 最高优先） | 制作人 2026-08-02 定稿：**先反馈，再精度**（"操作有回应" > 判定精确）；负责人 opencode；先出排查报告 → 反馈放大 → 精度放宽 → 探针 + 真机复测 | — |

## 观察中 / 待决策项（非 bug 但需留意）

| 事项 | 级别 | 说明 |
|------|------|------|
| 价格数据三源（`CROP_DEFS` / `Economy` / `ShopPanel` 硬编码） | P2 | 已列入《任务-非剧情执行计划-v0.5.2.md》批次 1-1，搁置中 |
| MapScene 上帝类（持续膨胀） | 架构 | 遵循 AGENTS.md：先提方案不重构；仅在稳定前提下渐进拆分 |
| 移动端 gameplay E2E | P1 | 待第二轮人工测试结论后决定是否立项 |
| 四感受打磨（v0.5.2 第 4 步） | — | 已批准范围待启动，与 bug 修复不冲突 |
| v0.6 美术视觉升级 | — | 任务卡草案已就绪（[任务-美术v0.6视觉升级.md](docs/tasks/任务-美术v0.6视觉升级.md)），待 v0.5.3/v0.5.4 收尾 + 制作人确认后启动；第一批顺序：UI 套件 → 林澈头像 → 夏雅立绘+sprite → 村长立绘（[顶层设计 §10.6 决策③](顶层设计.md)）；制作人真机反馈确认美术质量参差+立绘不够，方向已对齐 |
| 神秘少女无立绘 | — | 制作人真机体验反馈：主要角色无形象支撑影响沉浸感；涉及角色形象属剧情权限范畴，需制作人决策是否补立绘（Demo 不揭底，立绘非必需但体验加分）；现有立绘（林澈/夏雅）质量不够已并入 v0.6 任务卡 |
| 神秘少女「月之少女」气质方向 | — | 制作人 2026-08-03 拍板方案 A：月=气质补充，不推翻星轨守护者定稿（顶层设计 §10.9）；草案见 [世界观设定/神秘少女设定草案.md](docs/世界观设定/神秘少女设定草案.md)；Demo 不揭底 / v0.6 不开发 / 不改现有对白 |
| 音乐音效整体评估（粗制滥造，评分 1/5） | — | 制作人真机体验反馈最低分项；需评估 AudioSystem 整体方案（音源质量/触发逻辑/移动端兼容），先报告不直接改；与 v0.6 视觉升级解耦，可独立排期 |

## 复测指引

> 探针脚本已迁移至 `tests/probes/`，运行方式：`node tests/probes/<脚本名>.mjs`

- BUG-001/002/003：`node tests/probes/probe-mobile-sleep.mjs` + `node tests/probes/probe-mobile-tutorial.mjs`（全流程触屏驱动）
- BUG-004：`node tests/probes/probe-mobile-ux.mjs`（含横屏 844×390 用例）
- BUG-005：`node tests/probes/probe-mobile-ux.mjs`（P2a-c）
- BUG-007：真机竖屏观察（《[移动端人工测试方案.md](docs/reports/移动端人工测试方案.md)》S1/S7）
- BUG-008：真机"睡觉 → 杀进程 → 重开"（方案 S10）

## 登记记录

| 日期 | 内容 |
|------|------|
| 2026-08-02 | 初版：登记人工测试发现的 9 项问题 + 观察项，关联修复 commit 与探针 |
| 2026-08-02 | 评估 Trae 的 BUG-006 修复（`408d8f9`）：方向正确、已生效；残留 P2 交互陷阱登记见观察项 |
| 2026-08-02 | 登记《任务-移动端问题优先级排序-v0.5.2.md》：P0 第一次睡觉 Bug（BUG-012）+ P1 任务列表 UI / 右下角 UI 过大 + P2 UI 整体升级 / 异常行为测试清单 + v0.5.2 Mobile Polish 三阶段开发顺序 |
| 2026-08-02 | BUG-012 根因定位并修复：`refreshDailyQuests()` 跨天仅保留引导任务，已完成未领奖任务被静默替换（奖励丢失）；1 行修复（保留条件扩展为「引导任务或已完成且未领奖」），回归探针 `probe-bug012-dailyquest` |
| 2026-08-02 | 制作人真机复测反馈：①横屏手机仍显示键盘文案（实为 `isMobileLayout` 宽度误判，非缓存）②睡觉后教程提示条残留；定位并修复 BUG-016/017（`config.ts` + MapScene `SHUTDOWN` 清理），回归探针 `probe-fix-mobile-hint` 5/5，教程/睡觉/文本探针全绿 |
| 2026-08-02 | 制作人反馈三项美术/布局问题：①NPC 头顶名字看不清 ②农场 NPC 扎堆左上角 ③矿洞有石头卡碰撞区采不了。修复（`85ddd38`）：NPC 名字标签加 3px 黑描边+阴影（普通/清晨夏雅/教程夏雅三处）；farm 站位从 col3-10 散布全图六象限（town/forest/mine 本就分散未动）；矿洞 s1/i1 移出石簇（BUG-018）。验证：`probe-layout` 28/28（含全部 NPC 日程站位+全部矿点可通行）、`probe-density-v053`（E1/E3/E4）、`probe-density-v053-batch2`（E2/E5/E6）10/10、`probe-mobile-tutorial`、`probe-mobile-text` 6/6 全绿 |
| 2026-08-02 | 剧情密度第二批 E2/E5/E6 实现完成（`85ddd38` 部分 + `cf2e260` 数据）：E2 首次收获夏雅反馈（内存 flag 防重复）；E5 爷爷笔记可读物件（按天轮换，零存档字段）；E6 观星后少女追加（`isObservatoryComplete()` 条件）。`tsc` 通过，batch2 探针 10/10 |
| 2026-08-02 | 制作人移动端试玩反馈："砍树/挖矿引导体验不到剧情、任务流程有问题"。定位 BUG-019：爷爷笔记(label基准+位置)抢占砍树引导 + 挖矿引导触发时机突兀。修复：爷爷笔记移(1,6)且交互基准改椭圆实际坐标；挖矿引导改矿脉旁24px才触发；两引导扩为夏雅参与的7句剧情对话。验证：probe-guide-dialogue 6/6 + probe-note-vs-woodcut 2/2，回归 test-woodcutting 18/18、density 两批、mobile-tutorial/text、ch1-story 全绿 |
| 2026-08-02 | 制作人安卓真机体验反馈汇总（[安卓体验测试反馈收集 copy.md](安卓体验测试反馈收集 copy.md)）：评分 画面3/音效1/操作1/剧情3/角色3/完成度2。登记 6 项新问题：BUG-021（开场剧情被跳过·P0）/BUG-022（任务卡进度不让睡觉·P0）/BUG-023（跳过按钮失效·P1）/BUG-024（无删档按钮·P1）/BUG-025（横屏左边界漏洞·P1）/BUG-026（种植体验差专项·P1）。观察项追加：神秘少女无立绘（剧情权限·待决策）、音乐音效整体评估（1分·待立项）。美术质量参差+立绘不够已并入 v0.6 任务卡 |
## 评估记录：Trae 对"按 E 无法推进任务"的定位（408d8f9）

- **Trae 的定位**：引导任务（挖矿/砍树）混入每日任务池在教程期投放 → 玩家无斧头/未解锁矿洞，按 E 靠近树被"需要斧头才能砍树！"拦截 → 任务无法推进。
- **评估结论**：
  - ✅ 定位准确、修复方向正确：教程完成（storyStep=done、获得斧头）后才注入引导任务（`injectGuideQuests`），面板保持 4 个，未领奖跨天保留。
  - ⚠️ 它解决的是"**每日面板砍树/挖矿任务无法推进**"，与"**睡觉无法完成教程**"（BUG-001/002，由真实床铺 + 相邻判定 + 提前睡觉守卫修复）是**两个独立问题**；两者合并后移动端真实教程全流程探针 10/10 通过。
  - ⚠️ 残留 P2 陷阱：无斧头时在树旁按交互仍会 `return true` 吞掉该次交互（"需要斧头才能砍树！"）；BUG-006 修复后教程期触发概率≈0。✅ 已加固（`77af4ce`）：`tryChopTree` 无斧头改 `return false`，不弹提示、不吞交互，操作落到农田交互（或自然无响应）。
