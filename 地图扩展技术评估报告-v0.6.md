# 地图扩展技术评估报告 v0.6

> 前置勘察报告（只读分析，未修改任何代码/资源）
> 日期：2026-08-02
> 目标：确认当前地图制作管线是否支持 v0.6 地图扩展（海岸 / 深林）

---

## A. 当前地图架构总结

### A1. 地图文件清单（6 张 Tiled JSON，全部 `public/assets/maps/`）

| 地图 | 尺寸（格） | Ground 实际 gid | Walls 实际 gid | 备注 |
|---|---|---|---|---|
| gate | 30×20 | 1,2,3,4,6,7,8 | 3 | 一次性教程图 |
| farm | 40×25 | 1,5,7 | 3,6,8 | 唯一异尺寸；树木=精灵非瓦片 |
| town | 30×20 | 6,7 | 3,6 | |
| forest | 30×20 | 1,2 | 3,8,9,10,11,12 | **用到 gid 9-12，但 tileset 只有 8 格（见 F1）** |
| mine | 30×20 | 2 | 3,4 | 矿脉=精灵 |
| house | 20×15 | 6,9,10,11,12 | 3 | 家具 9-12 |

- 统一参数：16×16 瓦片、orthogonal、renderorder right-down、Tiled 1.9.2 格式
- 统一结构：每图仅 **2 个 tilelayer（Ground 底层 + Walls 上层）**，**无 object layer**

### A2. tileset 引用方式（关键机制）

- 所有 JSON 的 `tilesets[0]`：`name: "placeholder"`，`image: "../tiles/placeholder_tileset.png"`（224×16，14 格）
- 但**运行时该 image 路径被覆盖**：[MapScene.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/scenes/MapScene.ts#L218-L230) preload 加载 `assets/tiles/${mapKey}_tileset.png?v=6` 为纹理 `'tiles'`，再 `map.addTilesetImage('placeholder', 'tiles')` 按 **name 关联、用独立 tileset 渲染**
- 结论：**JSON 里的 image 路径只对 Tiled 编辑器有意义，运行时生效的是 `${mapKey}_tileset.png`**。tileset name 必须保持 `'placeholder'`（红线）
- 独立 tileset 实际格数（实测）：farm/forest/gate/mine/town = **8 格**，house = 12 格，placeholder = 14 格

### A3. 各图 tileset 匹配度（实测 gid 用量 vs tileset 格数）

| 地图 | tileset 格数 | 地图最大 gid | 匹配 |
|---|---|---|---|
| farm | 8 | 8 | ✅ |
| gate | 8 | 8 | ✅ |
| mine | 8 | 4 | ✅ |
| town | 8 | 6 | ✅ |
| house | 12 | 12 | ✅ |
| **forest** | **8** | **12** | ❌ |

### A4. collision 配置（代码内，非 Tiled）

[MapScene.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/scenes/MapScene.ts#L301) 统一执行：
- `wallsLayer.setCollisionBetween(3, 4)` → 石墙(3)/水(4)
- `wallsLayer.setCollisionBetween(9, 13)` → 树(9-12)/树桩(13)

不随地图区分。gid 语义（红线，不可变）：

| gid | 含义 | 碰撞 |
|---|---|---|
| 3 | 石墙 | ✅ |
| 4 | 水 | ✅ |
| 5 | 土壤（耕地） | ❌ |
| 6 | 木地板 | ❌ |
| 7 | 小路 | ❌ |
| 8 | 花 | ❌ |
| 9-12 | 树/松树/树干 | ✅ |
| 13 | 树桩 | ✅ |
| house Ground 9-12 | 床/桌/毯/书架 | 专用 |

### A5. object layer 使用方式

**完全没有。** 传送点不是 Tiled object layer，而是代码配置 [exits.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/data/exits.ts)：
- `MAP_EXITS[mapKey]`：ExitZone[] = 触发矩形（像素）+ target + spawn（目标场景像素坐标，出口内侧 3 格防回弹）
- `MAP_NAMES[mapKey]`：HUD 显示中文名
- 现有拓扑：森林↔农场↔小镇 与 森林↔矿洞（四环）+ gate→farm + house→farm

### 勘察发现（F 系列）

- **F1（现状缺陷）**：`forest.json` Walls 层有 47 格使用 gid 9-12（树），但 `forest_tileset.png` 只有 8 格 → 运行时帧越界，**森林树木不可见（或渲染异常），但碰撞仍生效（隐形墙）**。来源：commit `b39e344`（美术替换）只画了 8 格 tileset，未同步扩展地图树瓦片。
- **F2（管线缺口）**：farm/forest/gate/mine/town 五个独立 tileset **无生成脚本**，全部直接入库（commit `b39e344` 手绘）。仅 placeholder（gen_sprite_assets→gen_woodcutting 扩展）与 house（gen_house 从 farm_tileset 复制前 8 格 + 4 家具）有脚本。

---

## B. 新增海岸地图最低成本方案

### 结论：管线支持，成本低（约 4 类改动 + 1 个生成脚本）

**1. 生成脚本**（新，仿 [gen_map_assets.py](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/tools/gen_map_assets.py#L136-L172) `write_map` 模式）
- `tools/gen_beach.py`：输出 `beach.json`（建议 30×20）+ `beach_tileset.png`
- JSON 必须遵守：Ground/Walls 双层、tileset `name: 'placeholder'`、`imagewidth/imageheight` 与 tileset 实际格数一致
- tileset 按 gid 语义绘制：建议最小 8 格（gid 1-8，含 4=水），若需沙滩椰树等装饰用 gid 9-13 区间（自动继承碰撞，无需改代码）

**2. Phaser 接入（仅 3 处代码 + 1 处回退映射）**

| 文件 | 改动 |
|---|---|
| [main.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/main.ts) | 加一行 `new MapScene('beach')` |
| [exits.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/data/exits.ts) | 加 `MAP_EXITS.beach`（出口）+ `MAP_NAMES.beach='海岸'` |
| [AndroidBackHandler.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/systems/AndroidBackHandler.ts) | `BACK_TARGET` 加 beach 回退目标（如 `beach:'forest'`） |
| MapScene.ts | 无改动（通用逻辑已覆盖）；仅当有专属玩法（如拾取物）才按 mapKey 加分支 |

**3. 存档：零改动**
- `player.scene` 是动态字符串；`world/farm/story` 均与地图无关
- 若海岸新增剧情节点 → 需扩展 `STORY_STEPS`（[StorySystem.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/systems/StorySystem.ts#L26)），否则存档校验会拒绝；纯探索区则完全不用动

**4. 地图边界出口缺口**：`make_border(gaps)` 模式在指定边留缺口，与 exits.ts 触发矩形对齐（像素 = 格 × 16）

### 成本量级
- 生成脚本 ~80-120 行（复用 write_map 骨架）
- tileset：8 格像素瓦片（v2 规范，确定性绘制）
- 接入 3 处 + 存档零改动 → **最低成本路径已验证可行**

---

## C. 深林地图最低成本方案

### 结论：与海岸同构，但必须先修 F1（森林树瓦片缺失）

**1. 前置修复 F1**：forest_tileset.png 补齐 gid 9-12（或改回引用 placeholder 的 14 格树瓦片）。深林是"森林的延续"，若树瓦片本身不可见，深林照抄会直接继承该缺陷。

**2. 生成脚本**：`tools/gen_deepforest.py`（同海岸模式）
- `deepforest.json`（建议 30×20）+ `deepforest_tileset.png`
- **树瓦片可直接复用森林树的绘制（gid 9-12 语义相同）**，只需扩展格数到 12+（8 基础 + 4 树），避免重复造轮子
- 视觉差异化：可加 gid 14+（深色树木变体）——因 14+ 在碰撞区间 9-13 之外，若需碰撞须在代码加 `setCollisionBetween(14,14)`，属于唯一需动 MapScene 的情况

**3. 接入**：与 B 完全相同（main.ts / exits.ts / BACK_TARGET / 存档零改动）

**4. 新增出口拓扑**：深林 ↔ 森林（或深林 ↔ 海岸），在 exits.ts 双向配置即可，无需改通用逻辑

### 成本量级
- 比海岸多一步 F1 修复（约 0.5 个任务）
- 树瓦片复用 → tileset 绘制量低于海岸（水面是海岸新增需求，树已有现成）

---

## D. 瓦片升级双轨方案风险

（背景：`地图升级计划.md` M1 瓦片统一 + 本 v0.6 地图扩展并行）

### D1. 现状双轨冲突点
- **占位体系**：placeholder_tileset = 14 格（8 基础 + 6 木材/树，gen_woodcutting 扩展），是"完整"的 gid 语义实现
- **独立体系**：各图 8 格手绘 tileset，**不完整**（F1 已实锤：forest 缺 9-12）
- 双轨并行 = 同一 gid 在不同 tileset 下内容不一致 → 新地图照抄哪个体系都可能出错

### D2. 具体风险清单

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| R1 | **gid 布局漂移**：升级时重排 gid 顺序 → 碰撞区间错位、存档内 farm.tiles 引用错位 | 🔴 高 | gid 语义冻结为红线文档（A4 表）；升级只换像素不改编号 |
| R2 | **house 连锁**：gen_house.py 复制 farm_tileset 前 8 格 → farm 升级后 house 需同步重新生成 | 🟡 中 | farm 瓦片升级任务必须包含 house 同步（脚本已封装，成本低） |
| R3 | **JSON image 路径误导**：运行时用 `${mapKey}_tileset.png`，JSON 却指向 placeholder → 新人改错文件 | 🟡 中 | 保持 `name:'placeholder'` 不变；报告本文档明确此机制 |
| R4 | **格数不一致**：新 tileset 格数 < 地图最大 gid → 渲染越界（F1 即此类） | 🔴 高 | 生成脚本自动校验 `max_gid(data) <= tilecount`，CI 化 |
| R5 | **farm.tiles 存档耦合**：farm 瓦片编号（gid 5 土壤/6 木地板）被存档引用 → 升级不得改编号 | 🔴 高 | R1 的缓解覆盖；升级报告需单独确认存档兼容 |
| R6 | 并行 AI 同文件冲突（MapScene.ts 被两 Agent 同时改） | 🟡 中 | 地图升级（本 Agent）与美术（opencode）按文件划分领地，如 D3 建议 |

### D3. 建议的"双轨"正确姿势
- **轨道一（瓦片升级）**：只换 tileset 像素，**gid 编号、地图 JSON、存档格式全部不动**
- **轨道二（地图扩展）**：新增地图用**完整 gid 语义**（12-14 格 tileset），不裁剪基础格
- 两者唯一交点：farm_tileset（升级对象）+ gen_house.py（依赖）——协调文件级领地即可

---

## E. 推荐开发顺序

```
阶段 0（前置，1 项）
  0.1 修 F1：forest 树瓦片补齐（gid 9-12）── 否则深林继承缺陷，且当前森林存在隐形墙
      → 影响范围：forest_tileset.png 单文件（可顺带补齐到 12 格）
  0.2 固化《瓦片 gid 语义红线文档》（A4 表 + R1 缓解）── 防双轨漂移

阶段 1（双轨启动）
  M1 farm 瓦片升级试点（地图升级计划第一优先级）── 验证"只换像素不改编号"流程 + house 同步
  C1 海岸新地图（本扩展第一张）── 用完整 12+ 格 tileset，验证最低成本管线（B 方案）

阶段 2（扩展铺开）
  C2 深林新地图 ── 复用森林树瓦片 + 海岸验证过的管线（C 方案）
  深林/海岸出口拓扑接线（exits.ts 双向）

阶段 3（收尾）
  全图回归：Level 2 测试（tsc + 传送闭环 + 存档兼容探针）
  F2 管线缺口补强：为独立 tileset 补生成脚本（若 M1/C1 验证脚本化收益）
```

**关键判断**：
1. 当前管线 **支持 v0.6 扩展**，且新图接入成本极低（3 处代码 + 1 脚本 + 存档零改动）
2. **F1（森林树瓦片缺失）必须前置修复**——它是管线"格数不一致"风险（R4）的现行实锤，也是深林扩展的直接依赖
3. 传送点代码化（exits.ts）是本项目架构优势：新地图接入天然支持，无需 Tiled object layer
4. 双轨升级的安全边界 = **gid 编号冻结**；像素可换、编号不可动

---

### 附：勘察证据文件
- 地图 JSON：`public/assets/maps/*.json`（gid 用量已程序化统计）
- tileset 尺寸实测：placeholder=224×16(14格)、house=192×16(12格)、其余=128×16(8格)
- tileset 来源：独立 5 图来自 commit `b39e344`（无脚本）；house 有脚本（gen_house.py）
- 接入点：[main.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/main.ts) / [exits.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/data/exits.ts) / [MapScene.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/scenes/MapScene.ts) / [SaveSystem.ts](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/systems/SaveSystem.ts)
