# 美术规范（v2 像素风）

> 本文档是《归星物语》美术开发的唯一规范基准。所有新增/重绘美术资源必须遵守。
> 与 `任务-美术P0风格统一重绘.md`（Codex 任务卡）配套：任务卡给出单任务要求，本文档给出长期统一规范。

---

## 1. 风格基准

**v2 严格像素风**，参照实现：`tools/gen_sprite_assets.py`（玩家/NPC 角色）与
`tools/gen_npc_extras.py`（3 新 NPC + 矿石）。

- 逐像素绘制，**禁止**平滑椭圆 / 渐变 / 抗锯齿（PIL `ImageDraw.ellipse` 等平滑原语仅用于内部裁剪，不直接作为成图）
- 左上光源、右下投影
- 1px 深色外描边：统一 `C.OUTLINE = (18, 14, 22, 255)`，**不要用纯黑**
- 三色调 + 高光：每个色块 = base / mid / shadow（+ 少量 light），不做大色块平涂

## 2. 尺寸规范

| 资源类型 | 画布尺寸 | 运行时缩放 | 说明 |
|----------|----------|------------|------|
| 角色 NPC / 玩家帧 | 32×32 | 场景内 `setScale(0.5)` | 显示为 16×16，与 16×16 瓦片协调 |
| 物品图标 | 16×16 | UI 中 16~28px 显示 | `assets/icons/` |
| 瓦片 tileset | 16×16/格 | 1× | `assets/tiles/` |
| 场景物件（树/树桩） | 32×32 | `setScale(0.5)` | 显示 16×16，小尺寸下轮廓必须清晰 |
| 矿石 | 32×32 | 按类型 12/14/16 ÷ 32 | setupOres 内 setScale |

## 3. 调色板

统一从 `gen_sprite_assets.py` 的 `C` 类导入，**禁止重新发明 RGB**。

- 通用色：`C.SKIN`（所有角色共用肤色，保证种族一致）、`C.OUTLINE`（描边）、`C.BLACK` / `C.WHITE`
- 瓦片色：`C.T_*`（草/泥/石/水/土/木/路/花）
- 角色专属色：在各自脚本的局部类中扩展（如 `gen_npc_extras.py` 的 `N` 类、`gen_style_unify.py` 的 `T` 类），命名遵循 `{角色缩写}_{用途}` 语义

新增调色板时保持统一语义命名，例如 `MN_HELMET`（矿工安全帽）、`AV_JACKET`（冒险家夹克）。

## 4. 角色绘制规范

- 脸部**必须**复用 `gen_sprite_assets.py` 的 `draw_face_down_32`（6+ NPC 脸型一致）
- 32×32 角色结构（y 轴 0..31）：
  - `y 0..6` 头顶（头发/帽子/头饰）
  - `y 5..12` 脸部
  - `y 11..22` 上身（衣服/手臂/腰带）
  - `y 22..28` 大腿
  - `y 28..31` 小腿 + 鞋
- x 轴主体在 `x 8..23`（16px 宽），`0..7` 与 `24..31` 留给外延（长发/拐杖/镐/剑）

## 5. 生成管线（唯一方式）

**所有美术资源必须由脚本生成**，可重复运行、输出一致（无 random 抖动）：

| 脚本 | 产出 | 备注 |
|------|------|------|
| `tools/gen_sprite_assets.py` | 玩家 4×4、村长/商人/少女、占位 tileset | 风格基准 |
| `tools/gen_npc_extras.py` | 矿工/花匠/冒险家 + 3 矿石 | 复用 `C` 类 |
| `tools/gen_style_unify.py` | 树×2/树桩/斧/木/夏雅（v2 重绘） | 消除旧平滑风 |
| `tools/gen_item_icons.py` | 18 个 16×16 物品图标 | `assets/icons/` |
| `tools/gen_map_assets.py` | 各地图 + tileset | 瓦片类 |
| `tools/gen_xiya.py` | 夏雅（**已废弃**，改用 gen_style_unify） | v1 平滑风勿再用 |

新增资源：优先**扩展既有脚本**，不新建重复管线。脚本必须确定性（固定种子/无 random），保证可复现。

## 6. 文件布局

```
public/assets/
  sprites/   角色、场景物件（32×32）、矿石（32×32）、作物 spritesheet（32×32/帧）
  icons/     物品图标（16×16）
  tiles/     各地图 tileset（16×16/格）
  maps/      Tiled 地图 JSON
  images/    大图（封面/背景，非像素）
```

## 7. 资源接线规范（src）

美术贴图生成后必须接线才生效：

1. **preload**：`MapScene.ts` preload 中 `this.load.image(key, 'assets/sprites/xxx.png')`（物品图标用 `assets/icons/{id}.png`）
2. **NPC**：`NPCSystem.ts` 的 `textureKey` 指向贴图 key；preload 已兜底加载
3. **矿石**：`MapScene.ts setupOres` 用 `add.image(cx, cy, 'ore_{type}')` + `setScale`（不要用 `add.ellipse` 占位）
4. **UI 图标**：统一用 `Inventory.ts` 导出的 `itemIconHtml(id, size)` 渲染 `<img>`（只用于 `innerHTML` 场景；HUD 纯文本行保留文字）

## 8. 已知遗留（勿回退）

- HUD 状态栏 `seedInfo` 走 `textContent`，仍用作物 emoji 文字指示（像素图无法内联纯文本）
- `assets/sprites/player_backup.png`、`assets/images/Gemini_Generated_Image_*.png`（2.4MB）为孤儿文件，未被 src 引用，暂保留
- `npc_xiya` 在 gate 场景 `add.sprite` 未缩放（显示 32×32，略大于常规 NPC 16×16），属既有设计，勿动

## 9. 变更纪律

- 改 src 前先读相关文件，理解数据流，找最小修改点
- 不创建重复系统（有 `Inventory.ts` 就不建 `NewInventory.ts`）
- 改完必跑：`npx tsc --noEmit` → `npm run build` → tutorial 流程测试
