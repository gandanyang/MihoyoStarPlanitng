"""生成夏雅专用 NPC 贴图（32x32，女主级设计）"""
from PIL import Image, ImageDraw

img = Image.new('RGBA', (32, 32), (0, 0, 0, 0))
d = ImageDraw.Draw(img)

# 夏雅：温柔系女主
# 配色：深棕发 + 粉白连衣裙 + 淡紫发饰

# --- 头发（后层） ---
# 长发披肩
d.ellipse([10, 2, 22, 16], fill=(90, 50, 30))   # 头顶
d.ellipse([8, 4, 12, 20], fill=(90, 50, 30))     # 左侧长发
d.ellipse([20, 4, 24, 20], fill=(90, 50, 30))    # 右侧长发
d.rectangle([8, 10, 12, 22], fill=(90, 50, 30))  # 左发束
d.rectangle([20, 10, 24, 22], fill=(90, 50, 30)) # 右发束

# --- 面部 ---
d.ellipse([11, 6, 21, 16], fill=(255, 230, 210))  # 脸部

# --- 眼睛 ---
# 左眼
d.ellipse([13, 9, 15, 12], fill=(60, 40, 30))
d.ellipse([13, 9, 15, 11], fill=(255, 255, 255))  # 高光
# 右眼
d.ellipse([17, 9, 19, 12], fill=(60, 40, 30))
d.ellipse([17, 9, 19, 11], fill=(255, 255, 255))  # 高光

# --- 嘴（微笑） ---
d.arc([14, 13, 18, 15], 0, 180, fill=(200, 100, 100))

# --- 发饰（淡紫色蝴蝶结） ---
d.ellipse([14, 3, 18, 5], fill=(180, 140, 220))    # 蝴蝶结中心
d.polygon([10, 3, 12, 1, 12, 5], fill=(180, 140, 220))   # 左翼
d.polygon([22, 3, 20, 1, 20, 5], fill=(180, 140, 220))   # 右翼

# --- 前额刘海 ---
d.ellipse([10, 4, 14, 9], fill=(90, 50, 30))   # 左刘海
d.ellipse([18, 4, 22, 9], fill=(90, 50, 30))   # 右刘海

# --- 身体/连衣裙 ---
# 衣领
d.ellipse([12, 15, 20, 20], fill=(255, 220, 230))  # 上身
# 裙子
d.polygon([10, 18, 22, 18, 24, 30, 8, 30], fill=(255, 200, 220))
# 腰带
d.rectangle([12, 21, 20, 23], fill=(180, 140, 220))

# --- 手臂 ---
d.ellipse([9, 18, 12, 24], fill=(255, 230, 210))   # 左臂
d.ellipse([20, 18, 23, 24], fill=(255, 230, 210))   # 右臂

# --- 腿部 ---
d.ellipse([11, 28, 14, 32], fill=(255, 230, 210))   # 左腿
d.ellipse([18, 28, 21, 32], fill=(255, 230, 210))   # 右腿

# --- 鞋子 ---
d.ellipse([10, 30, 15, 32], fill=(140, 80, 60))    # 左鞋
d.ellipse([17, 30, 22, 32], fill=(140, 80, 60))    # 右鞋

img.save('public/assets/sprites/npc_xiya.png')
print('[OK] npc_xiya.png 已生成 (32x32)')