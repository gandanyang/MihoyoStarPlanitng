# 《归星物语》地图升级计划

**状态**：📋 草案（待制作人拍板）
**优先级**：🔴 **当前最高优先级事项**（制作人 2026-08-02 指示）
**日期**：2026-08-02
**依据**：制作人安卓真机反馈「画面 3/5、美术质量参差不齐」+ 顶层设计 §10.6 视觉升级第二批（场景氛围/天气/光影）

---

## 一、为什么是最高优先级

1. **地图是玩家第一眼印象**：进游戏第一个画面就是地图（车站 → 大门 → 农场），地图视觉直接决定"像不像独立游戏"
2. **画面 3/5 的直接靶点**：制作人反馈美术参差，地图 tileset 目前是程序生成/占位风格，是最大短板之一
3. **v0.6 第二批前置**：§10.6 决策③ 的「场景氛围 → 天气 → 光影」本就是视觉升级内容，提前 + 系统化为地图升级
4. **为 v0.7 铺路**：海滩新地图（复用 MapScene）依赖统一的地图配置模式

---

## 二、现状盘点

| 项 | 现状 |
|----|------|
| 地图 | 6 张 Tiled JSON：farm（40×25）、forest、gate、house、mine、town + station（代码绘制）+ 观星场景 |
| 瓦片 | 16px tile；每图独立 tileset PNG（`assets/tiles/*_tileset.png`）+ `placeholder_tileset.png` 兜底 |
| 渲染 | [MapScene.ts](src/scenes/MapScene.ts) `createScene()`：Ground(Walls 两层，wallsLayer 碰撞) |
| 碰撞语义（**不可变**） | 石墙 gid3 / 水 gid4 / 树 gid9-12 / 树桩 gid13（`setCollisionBetween(3,4)/(9,13)`） |
| 床格语义（**不可变**） | house=床铺 gid9、farm=木屋地板 gid6（`collectBedTiles`） |
| 氛围 | 无场景氛围 / 天气 / 光影（全部缺失） |
| 已知问题 | tileset 风格不统一、美术参差（真机反馈第 6 项）；操作/触控体验与地图细节相关（BUG-026） |

---

## 三、升级目标与任务分阶（最高优先 → 递减）

### 本轮范围（✅ 制作人已拍板：M1+M2 先行）

### M1 · 瓦片统一升级（最优先，农场试点先行）
- 6 图 tileset 统一升级：v2 像素风（16px、1px 描边、统一色板，见《美术规范》）
- **保持 gid 语义不变**（碰撞 3/4/9-13、床格 6/9）→ 只换外观，不动地图 JSON 布局与玩法
- 顺序：farm 试点 → 其余 5 图接续
- 产出：`assets/tiles/*_tileset.png` 新瓦片 + 生成脚本（`tools/gen_*`）

### M2 · 场景氛围
- 每图主题差异：农场暖阳、森林绿荫、矿洞昏暗、小镇暖灰、大门夕照
- 氛围参数集中配置（每图一份：天空 tint / 装饰层 / 色调）

### 顺延（拍板后置 v0.6.1）

### M3 · 天气系统
- 晴 / 雨 / 雪：粒子渲染 + 天空色变化，按天随机（与 gameTime 联动）
- 全局 WeatherSystem，复用 MapScene，性能有上限（粒子数封顶）

### M4 · 光影系统
- 昼夜过渡（白天 → 黄昏 → 夜晚 tint 渐变）+ 屋内暖光点光源
- 与现有时间系统联动，移动端低配降级

### M5 ·（v0.7）海滩新地图
- 复用 MapScene 配置化模式（一张地图 = 一份 JSON + tileset + 专属逻辑）

---

## 四、技术要点

- **低风险原则**：瓦片只换外观，gid 语义、地图布局、存档、碰撞全不动；新增地图专项探针校验（碰撞格 / 床格 / 出口）
- **系统集中化**：氛围/天气/光影做成 MapScene 可插拔渲染管线 + 每图配置，禁止复制场景（顶层设计 §7.2）
- **工具链**：瓦片由 `tools/gen_*.py` 生成（与现链一致，可重复输出），人工 PNG 不入库
- **红线**：不触碰剧情 / 存档结构 / 碰撞语义 / 顶层设计

---

## 五、决策点（✅ 制作人已拍板 2026-08-02）

| # | 决策 | 拍板结果 |
|---|------|----------|
| D1 | 本轮范围 | **M1+M2 先行**（瓦片统一 + 场景氛围）；M3 天气 / M4 光影顺延 v0.6.1 |
| D2 | 试点方式 | farm 先行（拍板默认采纳） |
| D3 | 天气/光影 | **顺延**（随 D1 一并拍板） |
| D4 | 执行分工 | **全部本 Agent**（瓦片生成 + 氛围系统） |
| D5 | 与 v0.6 第一批顺序 | **并行**（地图与 UI 文件不重叠） |

> 注：D4 拍板全部由本 Agent 执行，瓦片不再依赖 opencode 美术线——本计划即地图升级任务卡。

---

## 六、与在途工作衔接

- 并行 AI v0.6 UI 套件（`src/ui/*`、`ui-theme.css`）→ 与地图（`src/scenes/MapScene.ts` 渲染层 + `assets/tiles/*`）**文件不重叠**，可并行
- BUG-021~025 已修复（`7971122`）→ 阶段 0/1 大部分完成，地图升级接管最高优先级
- BUG-026 种植体验专项 → 与地图触控细节相关，可结合 M1 农场重做一起评估
- 美术任务卡（`任务-美术v0.6视觉升级.md`）已被并行 AI 清理/移动 → 地图升级需登记为新任务卡（或由美术线认领瓦片项）

---

## 七、风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| gid 语义被瓦片替换破坏（碰撞/床格失效） | 高 | 新增地图专项探针（碰撞格/床格/出口校验）；瓦片表 review |
| 天气/光影在低端安卓掉帧 | 中 | 粒子上限 + 低配降级（tint 直渲不叠加粒子） |
| 多 AI 领地冲突（`assets/tiles/*` vs `src/systems/*`） | 中 | 按 D4 分工，瓦片只由一方改；每项单独 commit |
| 地图升级拖累 v0.6 第一批进度 | 低 | 并行推进，地图不阻塞 UI 套件 |

---

## 八、验收标准

- [ ] M1 完成后：6 图瓦片风格统一，farm 试点前后对比明显（截图）
- [ ] gid 语义探针全绿（碰撞 / 床格 / 出口 / 观星）
- [ ] M3/M4 完成后：天气切换（晴雨雪）+ 昼夜过渡在真机流畅（≥30fps）
- [ ] tsc + `npm run build` + 现有探针全绿 + 真机复测
- [ ] 未触碰：剧情 / 存档结构 / 碰撞语义 / 顶层设计

---

## 九、变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-02 | v0.1 | 初版：制作人指示最高优先级；M1~M5 任务分阶 + 5 项决策点 |
| 2026-08-02 | v0.2 | 制作人拍板：D1=M1+M2 先行（M3/M4 顺延 v0.6.1）；D4=全部本 Agent；D5=与 v0.6 第一批并行 |
