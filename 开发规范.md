# AI Game Development Skill

## 项目内核原则：中国语境（制作人定稿 2026-08-03）

> **外在审美可以有日式 ACG 影响；但世界观、价值观、人与人的关系，一定要是中国语境。**

- ✅ 二游立绘 / 日式 UI / 动画演出：可以（玩家熟悉且喜欢的表现形式）
- ⛔ 内核不要变成日式轻小说；不做"中国皮、日本魂、欧美骨架"
- 项目一句话特色：**用二次元的美术语言，讲一个发生在中国乡村、属于中国情感和中国生活方式的故事**
- 学习对象不是"日本怎么写农村"，而是"中国人怎么看农村"——乡村振兴不是口号，背后是：年轻人和故乡的关系、老人与土地的关系、村庄是未来的一种可能

**每写完一个剧情，审核四件事：**

1. 中国人的说话方式（有没有翻译腔）
2. 中国人的家庭关系（爷爷、邻居、乡亲是不是像现实）
3. 中国乡村的生活方式（节气、赶集、串门、种地等细节）
4. 中国价值观表达（不用喊口号，通过人物和事件自然体现）

**具体化原则（防口号化）：**

> 不要写"我们要乡村振兴"。
> 写：村里年轻人大多出去打工，只剩老人和孩子 → 主角回来后，慢慢有人愿意回来帮忙、创业、开店 → 废弃的老院子重新有了灯火 → 节日时大家一起热闹起来。

具体的人和事，比口号更能让玩家感受到主题。

宇宙浪漫主义同理：宇宙是普世的，但表达是中国的——**仰望宇宙、顺应自然、人是天地间的一部分；重点不是"征服"，而是"连接"**（详见《世界观长期规划-归星宇宙浪漫主义》）。

---

## AI 美术资源生成（默认优先：gpt-image-2）

> 优先级（制作人定稿 2026-08-03）：游戏需要的高质量美术资源，
> **① gpt-image-2（在线 API，当前默认）→ ② ComfyUI（本地备选）→ ③ 其他方式（脚本像素管线等）**。

### 工具

- `tools/gpt_image_gen.mjs` — 生图主脚本（gpt-image-2 / Images API，带批准门禁）
- `tools/gpt_image_key.mjs` — API Key 安全存储（Windows DPAPI 加密，禁止明文入库/发到对话）
- 中转站地址与 Key 配置在 `tools/.env`（已被 .gitignore 忽略，不会入库）

### 流程（每次生图必走）

1. **写提示词**：必须包含 v2 像素风约束（1px 深描边 RGB(18,14,22)、三色调、左上光源）与角色设定色（见 `美术规范.md` / `skills/10-art-direction.skill`）
2. **零成本预演**：`node tools/gpt_image_gen.mjs --dry-run "提示词"`（显示接口地址 + 费用估算）
3. **批准门禁**：真实调用前脚本必须交互 y/N 确认（或显式 `--yes`）；**每次生图须制作人批准**
4. **生成**：`node tools/gpt_image_gen.mjs --yes --size 1024x1536 --quality medium "提示词"`（对话头像建议尺寸 1024×1536，质量 medium 起步）
5. **后处理**：居中裁正方形 → 缩放 512×512（对话框头像）/ 512×768（立绘），PNG
6. **入库接线**：放入 `public/assets/portraits/` → 更新 `StoryDialogue.ts` PORTRAIT_MAP → 同步探针 → `npx tsc --noEmit`
7. **验收**：制作人看效果；不满意调整提示词重新生成（再次走批准流程）

### 成本与安全

- 官方按 token 计费：1024×1536 medium 约 $0.08~0.15/张（中转站按各自定价，生成前脚本会先展示估算）
- 测试试水用 `--quality low`；正式资源 medium/high
- Key 用 `node tools/gpt_image_key.mjs set` 加密保存；**绝不把 Key 写进聊天/日志/明文文件**

### 实践记录（2026-08-03）

- 村长对话框头像：`public/assets/portraits/elder_ai.png`（512×512 正方形，gpt-image-2 生成，已接线 PORTRAIT_MAP，旧 `elder.png` 保留可回退）

---

## ComfyUI 美术资源生成

> 备选方案 ②：当 gpt-image-2 不可用 / 效果不达标 / 需要本地离线生成时使用。

本地地址：`http://127.0.0.1:8188/`

工作流：`workflow/` 目录下
- `anima_turboV10.json` — 基础工作流
- `anima_蕾米埃尔.json` — LoRA 工作流

调用方式：
- `POST /prompt` 提交工作流，返回 `prompt_id`
- `GET /history/{prompt_id}` 轮询完成状态
- `GET /view?filename=xxx&subfolder=xxx&type=output` 下载图片

工作流关键节点：
| 节点 | 作用 |
|------|------|
| 20   | 正向 prompt |
| 19   | LoRA 触发词 |
| 5/6  | 宽度/高度 |
| 22   | 文件名前缀 |

尺寸根据项目需求自适应，不固定。

后处理：
- 生成后用 Pillow 去白底（阈值 ~225）
- 保存为 PNG 透明背景

---

## 游戏素材规范

优先生成：
- PNG 透明背景角色
- Sprite Sheet 动画素材
- UI 素材
- 游戏背景图
- 道具素材

文件命名：`{类型}_{名称}.png`
示例：`char_idle.png`、`enemy_guard.png`、`coin.png`

---

## 角色一致性

生成同一角色多张图时：
- 固定角色描述词（服装、发型、颜色）
- 固定种子（seed）保持相似构图
- 使用同一 LoRA 或参考图
- 避免风格、配色随机变化

---

## Web 游戏技术路线

默认技术栈：HTML + CSS + JavaScript + Canvas

可选引入：Phaser.js（复杂项目时）

原则：
- 浏览器直接运行
- 优先完成可玩 Demo
- 项目结构简单
- 开发前确认是否需要手机端支持

避免：
- 不必要的后端
- 过度工程化
- Demo 阶段引入商业级系统

---

## 代码开发规范

修改代码前：
1. 分析现有结构
2. 说明修改方案
3. 只改必要代码
4. 测试核心功能

禁止：
- 随意重构无关代码
- 引入大量新依赖
- 修改已有正常功能

错误处理：
- ComfyUI 请求失败时重试 3 次
- 图片加载失败时用 Canvas 形状兜底
- 关键操作加 try-catch

---

## AI Agent 工作模式

职责：需求分析 → 游戏设计 → 开发 → 美术资源 → 测试

不只是写代码，要主动考虑：
- 游戏体验
- 所需素材及成本
- 开发流程

---

## 开发目标

用户想法 → AI 设计 → AI 开发 → ComfyUI 生成资源 → Web Demo → 快速迭代

目标：低成本、个人可完成的 AI 游戏开发流程
