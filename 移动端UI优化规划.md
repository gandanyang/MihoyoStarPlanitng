# 移动端UI优化规划 - 归星物语

> 文档类型：UI/UX设计规划  
> 创建日期：2026-08-02  
> 版本：v0.5.4（v0.5.3-B 后续迭代）
> 状态：实施中

## 本轮已完成（v0.5.4，2026-08-02）

**✅ 手机端种子切换（P0 补齐）**
- 农场 HUD 下方新增种子按钮（仅触屏显示），点击弹出 4 种作物选择器（萝卜/番茄/玉米/草莓）
- 选中即更新 `selectedCropType`，HUD 按钮同步显示当前种子 + 库存
- 桌面保留 R 键循环切换；选择器打开时冻结点击种田；Esc / 场景切换清理
- 触屏探针 15/15 PASS，`be02f7d`

**⏸ 工具条收缩（P1，单独规划）**
- 暂缓原因：现农场交互是"点一下按土地状态自动锄/种/浇/收"，无工具选择概念
- 引入锄/水/斧/镐工具模式会改动核心手感 + 教程/砍树/采矿联动，风险高
- 待单独设计轮：如何在不破坏现有自动判断的前提下收缩按钮区

**⏸ 按钮视觉升级（P2）**
- 现按钮为内联 cssText 透明半圆 + 纯文字，无图标/质感/按下反馈
- 方向已定：SVG 图标 + 渐变圆角矩形 + 按下反馈，`data-action` 标识保留不破测试

---

## 〇、本轮优先级（制作人拍板，2026-08-02）

下一阶段定位 **v0.5.4 移动端体验版**，重点：

1. 操作舒服
2. UI 干净
3. 角色更有辨识度

**P0：任务入口化（本轮先落地）**
- 像背包一样，点击任务图标打开任务面板
- 常驻每日任务面板收起，改为「任务按钮入口 ↓ 任务面板」
- 面板分类：主线 / 支线 / 日常 / 好感
- 完成任务未领奖显示红点

**P1：右下角操作区收缩**
- 右下角按钮收进，常驻只留 交互/背包/菜单
- 工具（锄/水/种/斧/镐）点击背包后展开

**P1：点击种田体验**
- 点击土地 → 角色移动 → 自动执行
- 已有点击种田 base（`handleFarmTap`）继续打磨

> 设计原则（制作人）：UI 干净 > 信息堆叠；按钮收起来而不是铺开；角色色 > 白色 > 背景 — 不要让 UI 比角色抢眼。

---

## 一、当前UI状态分析

### 1.1 触控操作区（TouchControls.ts）
- **虚拟摇杆**：左下角，130x130px容器，46px拇指，死区10px
- **交互按钮**：右下角，72x72px，绿色半透明，文字"交互"/"使用工具"
- **背包按钮**：交互按钮上方，52x52px，蓝色半透明，仅移动端显示
- **实现方式**：DOM覆盖层，z-index:100，支持触屏和鼠标事件

### 1.2 HUD信息显示（MapScene.ts）
- **左上角**：时间显示 + 经验条（180x8px）
- **中上部**：区域名 + 游戏信息（种子/金币/钻石/体力）
- **右上部**：任务目标文字
- **实现方式**：DOM覆盖层，z-index:5

### 1.3 每日任务面板（MapScene.ts）
- **位置**：移动端左上角（top:70px, left:8px），桌面端右上角
- **尺寸**：min(190px, 38vw)
- **内容**：标题 + 可领奖任务 + 进行中任务 + 已完成统计
- **交互**：点击领奖按钮

### 1.4 剧情对话框（StoryDialogue.ts）
- **位置**：屏幕底部居中，宽度90%，最大700px
- **高度**：最小120px
- **内容**：立绘区（56x56px/96x96px）+ 角色名 + 对话文本 + 提示
- **交互**：点击/空格/E推进，支持选项选择

### 1.5 种子选择器（MapScene.ts）
- **位置**：全屏居中弹窗
- **尺寸**：min(300px, 85vw)
- **内容**：种子列表 + 播种按钮 + 取消按钮

---

## 一之一、P0 任务入口化设计（v0.5.3-B，制作人拍板）

> 目标：像背包一样，点击任务图标打开任务面板；收起常驻每日任务卡片。

### 任务输入
- 移动端：右下操作区新增「任务按钮」（常驻小按钮，图标 📋）
- 桌面端：沿用键盘 `J`（新增快捷键）不影响既有 `InputManager`

### 任务面板（点击任务按钮打开，Drawer 式）
```
┌─────────────────────────┐
│  📋 任务          [×]    │
├─────────────────────────┤
│ [主线] [支线] [日常] [好感] │  ← 分类 Tab
├─────────────────────────┤
│  > 主线                    │
│    星之碎片：返回村长交付 👑│
│  > 日常                    │
│    🎁 与 3 位 NPC 对话 2/3 │
│    🎁 收集 5 木材 5/5 [领奖]│  ← 红点 + 领奖按钮
├─────────────────────────┤
│     已领取 1/4             │
└─────────────────────────┘
```

### 分类数据源
| 分类 | 数据源 | 当前可用 |
|------|--------|---------|
| 主线 | `QuestSystem.getQuestState()` / `getQuestObjective()` | ✅ |
| 支线 | （无数据源，占位） | ⏳ 预留 |
| 日常 | `DailyQuestSystem.getDailyQuests()` | ✅ |
| 好感 | （无数据源，占位） | ⏳ 预留 |

- 无数据的分类显示灰色 Tab（"敬请期待"），不报错
- 后续版本加入对应数据时只扩展该 Tab

### 红点（未领回报）
- 日常分类有 `completed && !claimed` 任务 → 任务按钮右上角红点数通知角标
- 领回后红点消失，实时更新

### 面板生命周期
- 打开/关闭复用现有 DOM 覆盖层模式（类似背包/商店）
- 打开时冻结玩家移动（与背包一致），关闭恢复
- 不改变存档结构（只读渲染每日/主线任务）

---

## 二、移动端UI问题清单

### 2.1 P0级问题（稳定性/可用性）
1. **触控精度问题**：72x72交互按钮在小屏幕上可能误触
2. **布局冲突**：右下角按钮区可能遮挡游戏关键信息
3. **对话框遮挡**：剧情对话时无法看到游戏场景

### 2.2 P1级问题（体验优化）
1. **按钮尺寸过大**：72x72交互按钮占用过多屏幕空间
2. **信息过载**：HUD同时显示时间、经验、种子、金币、钻石、体力
3. **任务面板遮挡**：左上角任务面板可能遮挡游戏内容
4. **触控反馈不足**：仅有15ms振动，视觉反馈不明显
5. **对话推进不直观**：移动端玩家可能不知道点击推进

### 2.3 P2级问题（表现提升）
1. **视觉一致性**：不同UI组件风格不统一
2. **动画缺失**：UI切换缺少过渡动画
3. **适配细节**：不同屏幕尺寸下的显示效果

## 三、优化方案

### 3.1 P0级优化（稳定性优先）
**目标**：确保核心功能可用，不影响游戏流程

1. **调整交互按钮位置和尺寸**
   - 将交互按钮从右下角移至右下偏上位置，避免与摇杆冲突
   - 尺寸调整为64x64px，保持足够触控面积
   - 增加按钮按下状态的视觉反馈（颜色变化+缩放）

2. **优化对话框显示**
   - 对话框背景透明度降低，允许看到部分游戏场景
   - 增加对话框关闭的明确提示

3. **防止布局冲突**
   - 设置UI安全区域，避免关键游戏元素被遮挡
   - 任务面板增加折叠/展开功能

### 3.2 P1级优化（体验提升）
**目标**：提升操作流畅度和信息可读性

1. **重新设计按钮布局**
   ```
   当前布局：
   [背包] 52x52
   [交互] 72x72
   
   优化布局：
   [菜单] 48x48 (新增)
   [背包] 48x48
   [交互] 64x64
   ```

2. **优化HUD信息层级**
   - 主要信息：时间、金币、体力（始终显示）
   - 次要信息：钻石、种子（农场场景显示）
   - 隐藏信息：经验条（点击角色头像查看）

3. **改进任务面板**
   - 移动端：可折叠卡片式，显示当前主要任务
   - 增加任务优先级标识（主线/支线/每日）
   - 完成任务时有庆祝动画

4. **增强触控反馈**
   - 按钮按下：颜色变化 + 轻微缩放(0.95) + 振动(15ms)
   - 操作成功：短暂高亮 + 音效反馈
   - 错误操作：红色闪烁 + 提示文字

5. **优化对话体验**
   - 增加"点击继续"的明确提示动画
   - 对话选项增加触摸反馈
   - 长按快进功能

### 3.3 P2级优化（表现提升）
**目标**：提升视觉品质和品牌感

1. **统一视觉风格**
   - 建立设计规范：颜色、字体、间距、圆角
   - 所有UI组件使用一致的视觉语言

2. **增加过渡动画**
   - UI面板打开/关闭：滑入/滑出动画
   - 数值变化：数字滚动动画
   - 任务完成：庆祝粒子效果

3. **优化适配方案**
   - 基于屏幕宽度动态调整UI元素尺寸
   - 横屏/竖屏自适应布局
   - 安全区域适配（刘海屏、挖孔屏）

## 四、设计规范

### 4.1 按钮规范
```
主要按钮（交互/确认）：
- 尺寸：64x64px (触控热区 72x72px)
- 圆角：50% (圆形)
- 背景：rgba(76, 175, 80, 0.7)
- 边框：2px solid rgba(255,255,255,0.6)
- 字体：Arial Bold 14px
- 按下状态：缩放0.95，亮度+20%

次要按钮（背包/菜单）：
- 尺寸：48x48px (触控热区 56x56px)
- 圆角：50%
- 背景：rgba(33, 150, 243, 0.6)
- 边框：2px solid rgba(255,255,255,0.5)
- 字体：Arial Bold 12px
```

### 4.2 面板规范
```
任务面板：
- 宽度：min(200px, 40vw)
- 背景：rgba(25,20,15,0.92)
- 边框：1px solid rgba(138,106,69,0.6)
- 圆角：12px
- 内边距：12px
- 最大高度：60vh，可滚动

对话框：
- 宽度：90%，最大700px
- 最小高度：120px
- 背景：rgba(15,15,25,0.95)
- 边框：2px solid rgba(255,255,255,0.15)
- 圆角：12px
- 底部间距：20px
```

### 4.3 字体规范
```
标题：Arial Bold 16px，颜色#ffd700
正文：Arial 15px，颜色#e0e0e0
提示：Arial 12px，颜色#666666
数值：Arial Bold 14px，颜色#4caf50
```

### 4.4 颜色规范
```
主色：#4caf50 (绿色，代表生命/成长)
次色：#2196f3 (蓝色，代表交互/功能)
强调色：#ffd700 (金色，代表奖励/重要)
警告色：#ff5722 (橙红色，代表错误/警告)
背景色：rgba(25,20,15,0.92) (深棕色，半透明)
```

### 4.5 动画规范
```
按钮按下：transform scale(0.95)，持续时间100ms
面板打开：transform translateY(100%)，持续时间300ms，缓动cubic-bezier(0.4,0,0.2,1)
面板关闭：transform translateY(100%)，持续时间250ms
数值变化：数字滚动，持续时间500ms
任务完成：粒子效果，持续时间1000ms
```

## 五、实施计划

### 阶段一：P0优化（1-2天）
1. 调整交互按钮位置和尺寸
2. 优化对话框背景透明度
3. 增加按钮视觉反馈
4. 测试触控精度和布局冲突

### 阶段二：P1优化（3-5天）
1. 重新设计按钮布局
2. 优化HUD信息层级
3. 改进任务面板交互
4. 增强触控反馈系统
5. 优化对话体验

### 阶段三：P2优化（5-7天）
1. 建立统一设计规范
2. 实现过渡动画
3. 优化多尺寸适配
4. 整体视觉升级

## 六、测试计划

### 6.1 功能测试
- 触控按钮响应测试
- 面板打开/关闭测试
- 对话流程测试
- 任务完成流程测试

### 6.2 兼容性测试
- 不同屏幕尺寸测试（4.7寸-6.7寸）
- 横屏/竖屏测试
- 不同浏览器测试（Chrome、Safari、Firefox）
- 不同操作系统测试（iOS、Android）

### 6.3 性能测试
- UI动画流畅度测试
- 内存占用测试
- 电池消耗测试

### 6.4 用户体验测试
- 新手引导完成率
- 任务完成时间
- 用户满意度调查

## 七、风险评估

### 7.1 技术风险
- DOM操作可能影响性能
- 不同浏览器兼容性问题
- 触控事件冲突

### 7.2 设计风险
- 过度优化可能破坏现有体验
- 新设计可能不符合玩家习惯
- 动画可能造成眩晕感

### 7.3 时间风险
- 优化工作可能超出预期时间
- 测试周期可能延长
- 需要制作人确认设计方案

## 八、成功指标

### 8.1 可用性指标
- 触控准确率 > 95%
- 任务完成率 > 90%
- 新手引导完成率 > 85%

### 8.2 体验指标
- 操作响应时间 < 100ms
- 动画流畅度 > 60fps
- 用户满意度 > 4.0/5.0

### 8.3 性能指标
- UI渲染时间 < 16ms
- 内存占用增加 < 10MB
- 电池消耗增加 < 5%

---

**文档维护者**：UI/UX设计师  
**最后更新**：2026-08-02  
**版本历史**：
- v1.0 (2026-08-02)：初始规划文档创建