# 任务卡：P0 美术风格统一重绘（v2 像素风）

> 指派对象：opencode（美术开发）
> 发起人：Codex（顶层设计 / 美术规范）
> 优先级：P0（风格统一，体验一致性）
> 状态：待执行

---

## 1. 任务目标

把 6 张 v1 风格（平滑圆角、无描边、层次少）的贴图重绘为 **v2 严格像素风**，与刚提交的
`npc_miner / npc_gardener / npc_adventurer / ore_*` 画风统一：

```
public/assets/sprites/tree1.png      阔叶树（场景物件，已在 src 使用）
public/assets/sprites/tree2.png      松树（场景物件，已在 src 使用）
public/assets/sprites/stump.png      树桩（场景物件，已在 src 使用）
public/assets/sprites/npc_xiya.png   夏雅 NPC（已在 src 使用，教程剧情角色）
public/assets/sprites/old_axe.png    旧斧头（物品图标，暂未接线，本任务只重绘）
public/assets/sprites/wood.png       木材（物品图标，暂未接线，本任务只重绘）
```

## 2. 背景：什么是 v2 风格基准

以 `tools/gen_npc_extras.py`（刚提交的 3 NPC + 矿石）为**唯一风格参照**：

- 画布 **32×32**，RGBA 透明背景
- **逐像素绘制**：优先 `px / rect / hline / vline / box_outline / add_outline` 助手，禁止平滑椭圆/渐变/抗锯齿
- **1px 深色描边**：统一用 `gen_sprite_assets.py` 的 `C.OUTLINE = (18, 14, 22, 255)`，不要用纯黑
- **三色调 + 高光**：每个色块 = base / mid / shadow（+ 少量 light 高光），不做大色块平涂
- 角色脸部复用 `draw_face_down_32`（从 `gen_sprite_assets.py` 导入），保证 6 个 NPC 脸型一致
- 共享调色板从 `gen_sprite_assets.py` 的 `C` 类导入，不要重新发明 RGB

## 3. 逐项设计要求

### 3.1 npc_xiya.png（最高优先级，教程主角）

角色设定：**夏雅，镇长助理 / 机械维修师，20 岁**（见 `剧情.md`）。

- 风格方向：橙金短发少女（对话色 `#f0a050` 对应发色/服装点缀）、工装感（背带/工具腰带可选，呼应机械维修师身份）
- 必须：复用 `draw_face_down_32`；1px `C.OUTLINE` 描边；三色调；整体观感与 `npc_miner` 同等完成度
- 禁止：沿用当前简陋版（439 字节那版是 v1 占位，直接废弃重画）

### 3.2 tree1.png / tree2.png（阔叶树 / 松树）

- 保持现有轮廓可读性（游戏内 `setScale(0.5)` 显示为 16×16，**小尺寸下必须能一眼分辨树干/树冠**）
- 树冠：三色调绿色（base/mid/shadow）+ 少量高光像素点
- 树干：三色调棕色 + 1~2 个树皮纹理暗点
- 整体 1px `C.OUTLINE` 描边，与人物/矿石同一套描边语言

### 3.3 stump.png（树桩）

- 顶面椭圆 + 年轮纹理（2~3 圈，用像素环而非平滑椭圆）
- 树皮侧面竖纹 2~3 条；三色调 + 描边
- 视觉上与 tree1/tree2 的树干棕色系一致（树倒了能看出"同一棵树"）

### 3.4 old_axe.png（物品图标）

- 斧柄深棕木色 + 斧头冷灰金属色 + 锈迹暗点（保留"旧"的质感）
- 三色调 + 1px 描边；构图居中、主体占满 32×32 的约 70%（16×16 显示时可读）

### 3.5 wood.png（物品图标）

- 2~3 根圆木堆叠，木纹 1~2 条 + 截面年轮圆点
- 暖棕系三色调 + 1px 描边；构图居中

## 4. 实现方式建议

推荐**更新生成脚本**而非手绘 PNG（与现有管线一致、可重跑）：

1. 新建 `tools/gen_wood_v2.py`（或扩展 `tools/gen_woodcutting_assets.py`），从 `gen_sprite_assets.py` 导入
   `C / blank_sprite / px / rect / hline / vline / add_outline` 等助手；
2. 夏雅复用 `draw_face_down_32`，仿照 `gen_npc_extras.py` 里 `npc_miner_frame_32()` 的结构写 `npc_xiya_frame_32()`；
3. 运行脚本，覆盖输出到 `public/assets/sprites/` 下对应 6 个文件。

## 5. 边界与禁止事项

- ✅ 允许修改：`public/assets/sprites/` 下 6 个 PNG；`tools/` 下生成脚本（新增或修改）
- ❌ 禁止修改：`src/` 任何文件（本任务不接线、不动 preload / textureKey / setupOres）
- ❌ 禁止修改：`public/assets/maps/`、`public/assets/tiles/`（场景瓦片不属本任务）
- ❌ 禁止删除/重命名任何现有文件（只覆盖这 6 个 PNG）

## 6. 验收标准（DoD）

- [ ] 6 个文件全部重绘为 v2 严格像素风，尺寸仍为 32×32、RGBA 透明背景
- [ ] 每张图均有 1px 深色描边（非纯黑）+ 三色调层次
- [ ] `npc_xiya` 脸型与其余 NPC 一致（同 `draw_face_down_32`）
- [ ] tree1/tree2/stump 缩放至 16×16 后轮廓仍清晰可辨
- [ ] 生成脚本可重复运行且输出一致（无随机抖动导致的每次不同）
- [ ] 游戏资源加载无报错（手动进 farm 场景看树/树桩，进 gate 看夏雅）
- [ ] 未改动 `src/`、`maps/`、`tiles/`

## 7. 完成后提交

- 单独一个 commit，信息建议：`art: v2 像素风重绘树木/工具/夏雅（风格统一）`
- 提交前先 `git status` 确认只包含本任务文件

## 8. 后续（不在本任务内，仅提示）

接线任务（preload + textureKey + setupOres 替换占位椭圆、物品图标替换 emoji）是独立的
"P0 接线闭环"任务，会在本任务验收通过后另行指派。
