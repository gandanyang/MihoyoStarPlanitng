# 农田系统 Skill

## 系统定位

农田系统是游戏核心循环。

核心链路：播种 → 浇水 → 生长 → 收获 → 经济循环

农田状态持久化进入 SaveSystem，不得直接 localStorage 管理农场地块数量（跨天成长由 TimeSystem 驱动。

---

## 当前功能

已支持：

* 土地状态（空 / tilled（耕地） / planted（种） / watered（浇水） / grown（成长）
* 播种（选种子 → 种植
* 浇水（浇水 → 浇水
* 跨天成长（睡睡觉触发）→ 日生长计数）
* 收获（作物到背包 → 收获
* 农场等级 & 进度（FarmProgress）

### 体力消耗

* 耕地：消耗
* 浇水：消耗
* 砍树：3 砍击 × 5 体力 = 15 体力 / 棵；不足提示「体力不足，砍不动树！」

---

## 设计原则

农田数据必须与地图显示分离。

✅ 正确：

* **FarmState**（保存 5×6 地块数据 → FarmData） → 保存进入存档）
* **MapScene**（负责渲染地图瓦片 → 地图渲染器渲染）

地图层逻辑在 MapScene 用 getTileAt/putTileAt，数据 → Tilemap 写回。

### 作物设计

不要写死作物以数据驱动扩展。

错误示例错误：
`if (seed === "萝卜") { price=40 推荐：
```ts
interface CropConfig {
 id: string;        // 作物 ID（如"萝卜 / 胡萝卜 / 土豆 / 草莓）
 growDays: number; // 生长天数
 sellPrice: number; // 售价
 sprite: string;  // tileset sprite
}
```

新增作物不要硬编码。

### 种子选择规则

优先使用 R 键选择的种子（R 已选择种子选择的种子；种子选择弹框仅在**所选种子耗尽后才弹出种子选择种子已耗尽）。

---

## 新增作物流程

1. 新增 CropConfig 条目
2. 添加种子物品
3. 添加成长规则
4. 添加收获奖励（金币/物品/体力）
5. 测试跨天 + 存档恢复 + 经济平衡

---

## 经济平衡原则

农场投入：时间 + 体力 + 金币

产出：金币 + 经验 + 剧情推进

避免：单一赚钱方式破坏经济。
