# 剧情系统 Skill

## 系统定位

剧情系统负责《归星物语》的核心二游体验。

目标：让玩家感受到**角色 + 世界 + 情绪，而不是简单任务列表。

---

## 当前剧情结构

* **序章**：城市失业 → 收到通知 → 车站返乡 → 抵达归星谷

* **第一章**：入小镇 → 相遇 → 观星夜 → 主线推进 → Demo 结局（观星夜触发观星夜 + 章节结算观星。

v0.5.3 剧情密度增强：E1-E6 六个事件（+引导剧情扩为 7 句 + NPC 名牌美化。

---

## 剧情权限规则

剧情权限涉及以下内容，Agent 不得自行扩写或改变：

1. 主角成长
2. 角色身份
3. 世界观真相
4. 关键剧情节点
5. 玩家选择

优先级：顶层设计.md > 剧情开发规划文档 > 实现需求。Design Authority 冲突时：制作人最终决策优先 > 顶层设计.md 当前版本 > 功能规划 > Agent 建议。

废弃字段 / 方案已提交的制作人决策优先于历史文档方案。恢复废弃必须制作人重新确认。

---

## 剧情设计原则

剧情必须服务：

* 世界观
* 人物塑造
* 玩家目标

禁止：为发任务而写流水账。

---

## 对话系统 StoryDialogue

StoryDialogue 负责：

* 展示文本
* 控制推进（点击继续选项）

不要：在 UI 里写剧情逻辑（把 StorySystem 负责，StoryDialogue 只负责显示和推进。

### 接入立绘

PORTRAIT_MAP 立绘映射：linchen / xiya 已接入，其他 NPC 待补。

---

## 新剧情四要素（缺一不可）

新剧情必须写之前必须有四要素：

1. 触发条件（什么时候触发生效。
2. 场景生命周期（什么时候出现 / 消失）。
3. 是否影响存档（StoryStep 进度）。
4. 是否影响测试节点。

---

## 新剧情流程

1. StoryStep 设计（存档推进的节点）
2. Dialogue 对话文本数据
3. 触发条件（时间 / 地点 / Quest / NPC / 好感等）
4. 奖励（物品 / 金币 / 剧情推进
5. 进入存档 StoryProgress
6. DailyQuest 面板 updateDailyQuestPanel() 手动刷新 UI 更新

### 触发每日任务进度必须手动刷新（对话等事件触发后必须 updateDailyQuestPanel() 确保 UI 立即更新。

---

## AI 生成剧情要求

保持：

* 林澈人设（27 岁返乡程序员，内敛疲惫）
* 归星谷氛围（治愈 / 慢节奏 / 人与人连接）
* 二游节奏（张弛有度，不流水账

避免：流水账 NPC 对话。
