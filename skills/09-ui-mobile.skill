# 端与 UI 端 Skill

## 目标

游戏必须同时支持：

* PC 浏览器（键鼠）
* 手机浏览器（触屏）
* 安卓 APK（Capacitor 打包 WebView）

---

## UI 原则

UI 必须：

* 自适应屏幕（800×600 内部分辨率缩放到全屏
* 支持触摸
* 不遮挡游戏区域
* 信息分级（PC 完整信息，移动端精简两行）

### 移动端硬约束

1. UI 布局决策必须使用统一函数 config.ts 的 `isMobileLayout()` 函数。
禁止散落的 `window.innerWidth` / `window.outerWidth` 判断。

2. 移动端 HUD 不得显示键盘提示（WASD / E 交互 / 背包 / ESC 提示）。
虚拟控件承担教学。

3. 对话框移动端底部固定底部居中：
```
y = scale.height - 180
setScrollFactor(0)
```
（防止相机问题。

---

## 操作映射

|  | PC 键鼠 | 手机触屏 |
|---|---|---|
| 移动 | WASD / 方向键 | 虚拟摇杆 |
| 交互 | E | 交互按钮（右下大按钮，右下角） |
| 背包 | B | 背包按钮 |
| 切换种子/工具 | R | 切换按钮 |
| 菜单 | ESC | 菜单按钮 |
| 睡觉 | 走到床上 E 走到床上 + E 交互 |

新功能设计任何交互必须同时设计键盘 + 触屏双方案。

---

## UI 开发规则

UI 面板：

* 只负责显示
* 调用 System 接口修改 System / 事件回调）

不要直接修改核心数据。

### 面板

* BackPackPanel（背包）
* ShopPanel（商店）
* QuestPanel（任务抽屉式，分类 Tab。
* StoryDialogue（对话 + 立绘
* EndingPanel（结局结算）
* HUD（HUD（钻石体力金币等。

### UI 色板

主色 #4caf50 / 次色 #2196f3 / 强调 #ffd700 / 警告 #ff5722 / 背景 rgba(25,20,15,0.92)。

---

## 物理返回键（APK）

安卓物理返回键拦截为层级：关闭面板 → 关闭面板 → 上一场景 → 退出 App。
（Capacitor App 插件实现）。

### 全屏沉浸（建议）

StatusBar 插件隐藏状态栏/导航栏。
