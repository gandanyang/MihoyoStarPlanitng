# 测试规范 Skill

## 系统定位

保证游戏长期迭代稳定不回退。

使用 Puppeteer E2E 测试。

---

## 测试目标

测试真实玩家全流程：

启动游戏 → 移动 → 交互 → 完成任务 → 存档 → 重新加载。

---

## 测试分级（TEST_RULES.md Level 0-3）

| 级别 | 场景 |
|---|---|
| Level 0 | 纯文本 / 文档 / 注释 → 仅检查格式 |
| Level 1 | UI / 数值 / 单个系统小修改 → tsc + 相关模块探针 |
| Level 2 | 新功能 / 核心逻辑修改 → 相关系统测试 + 关键流程 |
| Level 3 | 大版本 / 存档结构 / 核心重构 → 完整测试 |

禁止：默认跑所有测试。每次修改前分析影响范围，判断等级。

### 修改前必须回答：「这个修改可能破坏什么？」

---

## 已有测试探针（tests/probes/）

* `test-tutorial.mjs — 新手教程 11 步驱动
* test-ch1-story.mjs — 第一章剧情
* test-woodcutting.mjs — 砍树
* test-stress-switch.mjs — 切图压力
* probe-mobile-*.mjs — 移动端专项探针

探针运行方式：

```
# 先启动预览服务器
npm run preview
node tests/probes/<脚本名>.mjs
```

---

## 新功能测试要求

任何新增系统至少包含三类测试：

1. **基础测试**：功能能不能跑
2. **边界测试**：重复点击 / 重复领取 / 切场景 / 刷新页面乱操作
3. **存档测试**：退出重开 → 数据恢复
---

## AI 改代码后流程

必须执行：

1. `npx tsc --noEmit`
2. `npm run build`
必要时：运行对应 E2E 探针。

---

## 测试原则

不要只测"能不能用"。

还要测：玩家乱操作"会不会坏"。

---

## 报告/体验回归

移动端真机测试方案见 docs/reports/移动端人工测试方案.md。
制作人真机反馈 → 登记问题追踪。
