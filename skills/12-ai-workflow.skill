# AI 协作开发工作流 Skill

## AI 角色定位

AI 不是独立程序员。AI 是项目成员。

---

## 工作前必须读取

开工前读取：

1. AGENTS.md（项目 Alpha 阶段开发指南 — 优先级 P0/P1/P2 与禁止事项）
2. 00-project.skill
3. 01-architecture.skill
4. 对应功能的专项 skill
5. `git status` 查看其他 AI 在途文件，避让领地。

---

## 修改三步流程

### 第一步：分析

先回答：

1. 当前代码在哪里？
2. 修改影响什么？
3. 是否影响存档？
4. 风险等级？

### 第二步：设计

提出文件修改列表。

示例：

```
修改：
- FarmState.ts（新 cropConfig 字段
新增：
- CropConfig.ts（作物配置）
测试：
- tests/probes/test-farm-crops.mjs
```

### 第三步：实现

要求：小步修改。

禁止：一次重写大量文件。

---

## 代码修改规则优先级

1. 优先：新增
2. 其次：局部修改
3. 最后：重构（架构问题先提方案不直接重构

修改规则

每次修改前：

1. 先阅读相关文件
2. 理解现有数据流
3. 找到最小修改点

不要"为了方便创建重复系统（已有 Inventory.ts 不建 NewInventory.ts，已有 StorySystem 不建 DialogueManager2.ts）。

---

## 每次提交说明必须包含

1. 修改内容
2. 修改原因
3. 是否影响存档
4. 是否需要测试
5. 测试结果

---

## 严格禁止行为

禁止：

* ❌ 删除未知代码
* ❌ 修改无关文件（领地避让）
* ❌ 擅自改变项目架构
* ❌ 引入大型依赖
* ❌ 新增战斗 / 抽卡 / 大地图 / 复杂养成 / 后端
* ❌ 替换 Phaser 技术方案
* ❌ 擅自剧情权限内容（主角成长/角色身份/世界观真相/关键剧情节点
* ❌ 未经制作人批准新增存档字段
* ❌ 自扩剧情（

---

## 新功能评价标准

不是「代码能跑」。而是：是否符合长期游戏开发。

是否符合：游戏体验：稳定 > 新功能，体验 > 系统数量，完成闭环 > 十个半成品。
