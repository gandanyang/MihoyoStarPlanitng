# 《归星物语》v0.6 版本计划

**状态**：📋 草案（待制作人拍板）
**日期**：2026-08-02
**前置**：v0.5.4 移动端体验已收尾（验收 24/24 通过）→ APK 打包已落地 → v0.6 启动

---

## 一、版本定位

**v0.6 = 视觉升级**（[顶层设计 §10.6 决策①③](顶层设计.md)，制作人 2026-08-02 拍板）

> 立绘收益受剧情密度约束——"先认识她，再被她的美打动"。v0.5.3 剧情密度增强已落地，启动美术升级的时机成立。

**本版本一句话目标**：让《归星物语》从"能玩的像素 Demo"升级为"看着像独立游戏 Demo"。

---

## 二、已拍板范围（硬性，不可调序）

### 第一批（高收益，按序执行）

| 序 | 项 | 目标 | 状态 |
|----|----|------|------|
| 1 | **UI 套件**（HUD/对话框/任务/背包/商店） | 替换 emoji 混搭（💠⚡🥕）为像素图标 + 统一面板色板 | 任务卡 [任务-美术v0.6视觉升级.md §3.1](任务-美术v0.6视觉升级.md) |
| 2 | **林澈头像**（512×512 半身） | 接入标题/存档/HUD/对话框 | 任务卡 §3.2 |
| 3 | **夏雅立绘 + sprite 升级** | 覆盖 gate/farm/E1 三处形象一致 | 任务卡 §3.3 |

### 第二批（第一批完成后接续）

| 序 | 项 |
|----|----|
| 4 | 村长立绘（§10.6 附加决定：暂缓一个迭代） |
| 5 | 标题界面视觉升级 |
| 6 | 场景氛围 / 天气 / 光影 |
| 7 | 其余 NPC 立绘（神秘少女/老张/小梅/阿风/商店老板） |

**边界**：美术升级整体不动剧情、不动存档结构、不新建生成管线（扩展 `tools/gen_*.py`），v2 像素风不可回退。详见任务卡 §5。

---

## 三、候选范围（旧 §10.2 内容扩展 — 与决策①冲突，需拍板）

[顶层设计 §10.2](顶层设计.md) 原规划 v0.6 = 内容扩展，但 §10.6 决策① 已把 v0.6 改为视觉升级。以下内容仍登记在案（[需求文档 §13.2](需求文档.md)、风险登记册"按需补入"）：

| 候选 | 说明 | 与视觉的关系 | ✅ 拍板结果 |
|------|------|--------------|-------------|
| 钓鱼 + 海滩地图 | 自包含模块，复用 MapScene；海滩 = 新地图 | 独立，可并行开发 | **后置 v0.7**（D3） |
| 好感度（轻量） | 对话变体 + 少量小任务，禁止复杂养成 | 影响 NPC 系统 | **后置 v0.7**（D4） |
| 工坊（木工/金属加工） | 中期金币消费出口 | 新增 UI + 系统 | ⏳ 待立项（D1 并行原则覆盖） |
| 第二章剧情（星坠的秘密） | 钓鱼/好感度/工坊/海滩/夏雅背景 | 需要剧情设计，触及剧情权限 | ⏳ 待立项（需设计轮） |

**风险登记册提醒**："内容不足撑不满 30 分钟" → 试玩实测后按需补入。

---

## 四、里程碑拆分（✅ 制作人已拍板 2026-08-02）

```
v0.6 前置  APK 遗留补齐（物理返回键 + Release 签名 + 真机冒烟）★ D5 已定
v0.6.0     美术第一批（UI 套件 → 林澈头像 → 夏雅立绘+sprite）
v0.6.1     美术第二批（村长立绘 → 标题界面 → 场景氛围/天气/光影）
并行内容线  工坊 / 第二章剧情（待细化立项）★ D1 已定并行原则
v0.7       钓鱼+海滩、好感度（轻量）★ D3/D4 已定后置
```

---

## 五、决策点（✅ 制作人已拍板 2026-08-02）

| # | 决策 | 拍板结果 |
|---|------|----------|
| D1 | v0.6 范围 | **B 视觉+内容并行**（美术线 + 内容线并行推进） |
| D2 | 第二批美术是否留在 v0.6 | 按 §10.6 决策③ 第一批优先，第二批视迭代节奏（未单独拍板） |
| D3 | 钓鱼系统启动时机 | **后置 v0.7** |
| D4 | 好感度轻量版 | **后置 v0.7** |
| D5 | APK 遗留项归属 | **v0.6 前补齐**（物理返回键 + Release 签名 + 真机冒烟） |

> 注：D1 并行原则已定，但两大内容模块（钓鱼/好感度）均已后置 v0.7，内容线当前实际候选 = 工坊、第二章剧情（触及剧情权限，需设计轮）。建议下一轮细化内容线立项。

---

## 六、协作与领地（多 AI 并行）

- 美术实现由 opencode/trae 按任务卡执行；**本计划不改变任务卡领地约定**（任务卡 §5）
- 若选方案 B，内容线（钓鱼/好感度）与美术线文件不重叠（钓鱼→新模块 `src/systems/FishingSystem.ts` + 新场景；美术→`src/ui/*` + `public/assets/*`），冲突可控
- 开工前 `git status` 避让在途改动（任务卡 §0 纪律）

---

## 七、风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| 美术线多 AI 并行改 `src/ui/*` 冲突 | 中 | 任务卡 §0 领地避让；每项单独 commit |
| 内容扩展（方案 B）稀释视觉迭代 | 中 | 决策点 D1；资源按优先级排 |
| 第二章剧情触及剧情权限 | 高 | 严格走 顶层设计 → 剧情规划 → 实现 流程 |
| APK 真机问题拖期 | 低 | 方案 A 把冒烟放收尾，随时可提前 |

---

## 八、验收标准（v0.6 整体）

- [ ] tsc + `npm run build` 通过
- [ ] 现有探针全绿（bug012 / density / tutorial / mobile / stargaze）
- [ ] v2 像素风统一（逐像素 / 1px 描边 / 三色调）
- [ ] 生成脚本可重复运行输出一致
- [ ] 未动剧情/存档/顶层设计（制作人领地）
- [ ] 移动端硬约束保持（对话框位置 / HUD 分级 / 触控区）
- [ ] 每项美术单独 commit，CHANGELOG 同步

---

## 九、变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-02 | v0.1 | 初版：汇总 §10.6 已拍板范围 + 候选内容 + 5 项决策点 |
| 2026-08-02 | v0.2 | 制作人拍板记录：D1=视觉+内容并行；D3/D4=钓鱼/好感度后置 v0.7；D5=APK 遗留 v0.6 前补齐；里程碑更新 |
