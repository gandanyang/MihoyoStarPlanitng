# 2026-08-06 工作日志

## 生活化幽默系统文档整理

- 将制作人提供的"生活化幽默与叙事调味系统"参考文本整理为规范设计文档，落盘至 `docs/design/生活化幽默系统-v0.1.md`。
- 整理内容：统一标题层级（原文本 # / ## 混乱）、删除冗余空行与横线、补文档元信息头（版本/状态/适用范围/优先级）、类型库 A-E 改为 ### 子标题、角色定位改表格、密度控制改表格、补交叉引用关联文档列表。
- 核对一致性：与《剧情规范》8 铁律、《世界观叙事原则》、《NPC 设计规范》、《林澈人物圣经》（"偶尔冷幽默，不做段子手"）全部对齐，无冲突。
- 在《剧情规范》关联文档列表中新增交叉引用，指向 `生活化幽默系统-v0.1.md`。
- 文档状态：📋 已归档，待制作人拍板后生效。

## 工作区回归检查（Level 2，14:00-14:20）

**背景**：未提交工作区含 FEATURE-039 智能出售（Economy/Inventory/ShopPanel/BackpackPanel/SaveSystem lockedItems 可选字段 + 新增 SmartSellPreviewPanel.ts）+ 41 条夏雅重录 wav。视觉升级（VIS-01/02）已在回归期间被提交（`70dc62a`，MapScene +350 行）。

**结论：无功能回归，3 个探针过时**（tsc 0 错）：
1. `probe-sell-all` 8 失败 = 探针过时：FEATURE-039 把「一键出售二次确认框」改为「SmartSellPreviewPanel 预览面板」（确认按钮接线 sellAllSellable 正常，面板 156 行完整）。探针仍找旧确认框。
2. `probe-voice` 8 失败 + `probe-bug039-voice-sync` 1 失败 = 探针过时：VoiceBank.find() 统一返回 `audio/voice_normalized/`（无回退，VoiceBank.ts:92），探针仍监听旧路径 `/audio/voice/`，必然 0 请求。
3. `probe-farm-restore` 首跑 12 失败 → 重跑 25/25 全绿：偶发时序（撞上视觉提交写入），非回归。

**待办**：3 个探针需按新实现更新断言（voice_normalized 路径 / 预览面板交互），属 Level 1 探针维护。

## 五任务执行（14:30-16:10，提交 2bb7c74/1dec0db/c2f55ad/bfa06d0）

**1. 修 3 个过时探针**（提交 c2f55ad）：
- probe-sell-all：交互改 #smart-sell-panel 预览面板确认；期望 228→215（stone/wood 改 reserve 保留不卖）；19/19
- probe-voice / probe-bug039-voice-sync：监听 /audio/voice/ → /audio/voice_normalized/（VoiceBank 无回退）；11/11、10/10

**2. 提交 FEATURE-039**（2bb7c74）：Economy/Inventory/SaveSystem(lockedItems 可选字段)/ShopPanel/BackpackPanel + 新面板 SmartSellPreviewPanel（156 行完整）

**3. CONTENT-01 森林事件**：确认童年记忆闪回已实现（9bf2ad8），实际工作是——
- **修复真 bug（P1）**：triggerDailyEvent 无对话守卫，交付 SHARD_DELIVER_DIALOGUE 播放中被阿风事件覆盖 → 观星引导台词丢失。修复：isOpen 守卫（bfa06d0）
- probe-ch1-walkthrough/probe-stargaze 同步闪回 overlay 推进 + camera.pan 2s 等待 + 结算面板动画链等待

**4. GUIDE-01**：确认已完成（花园 80px 首次提示 + 三阶段台词），仅更新任务池状态

**5. NPC-01 阿风生活化对白**（bfa06d0）：
- 日常池 +3 条（冒险日志意象/看明白才算冒险/走错路撞见好东西）
- DailyEventSystem 新增「阿风借出冒险日志」事件（15-19 时，6 行）
- 文案遵循《生活化幽默系统-v0.1》（自嘲式黑色幽默）

## FEATURE-040 剧情回顾系统（20:05-20:30，提交成功）

- 新增 `src/systems/DialogueHistoryManager.ts`（环形缓冲 MAX=50，模块级单例，不写存档）
- 新增 `src/ui/DialogueHistoryPanel.ts`（半透明覆盖层，zIndex 560 > StoryDialogue 500，Esc/点空白/关闭按钮关闭，内心独白斜体灰字）
- `src/ui/StoryDialogue.ts`：showLine 记录非选项行；右上角「剧情回顾」按钮（Skip 左侧 right:104px，pointerdown+click 双绑定）；面板打开期间暂停打字机+拦截 advance/skip，关闭后恢复进度
- 红线遵守：StorySystem/SaveSystem/MapScene 零改动
- probe-dialogue-history 13/13 全绿（关键：推进≥51 行后历史挤到 50，最旧挤出新行保留）
- 探针调试心得：面板关闭时 #dh-list 是旧 DOM 内容，验证历史须重新 open 面板；推进 60 行全推完对话 completed 后 openHistory 直接 return，须留几行不推完

## 宣发美图 round2（19:56-20:00，ComfyUI）

- 按任务卡方法（tools/_tmp_comfyui.mjs + 本地 ComfyUI 127.0.0.1:8188）生成 3 张：round2_01_garden（林澈+夏雅种田）、round2_02_xiya_rain（夏雅雨伞）、round2_03_afeng_log（阿风冒险日志）
- 种子用 crypto.randomInt(0, 2**32-1) 完全随机（2333507646/2756133798/2887280566），与上次不同
- 输出 public/assets/images/promo/round2_*.png，已提交（da9a6e6）

## git 仓库对象损坏（20:20 发现并处理）

- 提交 round2 图时 git 报 `invalid object ... for 'public/audition_afeng_v2.html'`——blob 对象缺失（仓库历史 gc 残留损坏）
- 修复：`git hash-object -w <file>` 强制把工作区文件写回对象库，随后提交成功（da9a6e6）
- 残留：`git fsck --full` 仍有多个 broken link（历史孤儿对象，`git log --find-object` 无归属 commit），不影响 HEAD/日常提交；未重写历史（稳定优先）

**调试心得**：探针竞态类失败（观星结算面板点不动）用「点击前等动画链结束」解决；CSSStyleDeclaration display setter patch 在 Chrome 上无效（IDL 属性），改用 MutationObserver + 轮询 trace 定位。

## FEATURE-041 归星录·相簿（20:45-21:20，代码完成待 git 恢复后提交）

- 新增 `src/data/PhotoAlbum.ts`（PHOTO_DATABASE 3 张 + unlockPhoto 幂等 + 存档 album 顶层可选字段）
- 新增 `src/ui/PhotoAlbumPanel.ts`（z220 半透明覆盖层，未解锁剪影占位，复用 panelFadeIn/Out）
- MapScene 接线：HUD「📖 归星录」按钮（top:78px 所有场景）+ 冻结 + handleBackButton 层级 + 3 解锁点（花园恢复/挖矿/老树 shardCount≥2）
- 图片：3 张 1280×720 webp（ffmpeg libwebp q80，38-75KB），源=round2/round3 宣发图
- SaveSystem album 字段（+6 行纯相簿代码，无他人改动）
- 探针 probe-photo-album 10/10；回归全绿；tsc 0 错
- 设计文档：docs/design/归星录-相簿系统设计-v0.1.md

## ⚠️ git 仓库并发损坏事件（21:00-21:20，codex 恢复中）

- 现象：提交相簿时发现另一个 AI 会话（TRAE/Codex）并发操作仓库——reflog 显示对方提交 3 次（视觉方案 6f14bc0e/支线方案 4c7ea987/青禾镇生活化 a2e8216）后 main 指向 a2e8216 但 commit 对象缺失
- 恶化：对象库持续丢失（我提交的 81b0eeb FEATURE-040 也从完好变为读取失败）；pack 目录有 tmp_pack（对方 gc 中断痕迹）
- 决策：**停止一切 git 写操作**（重置/stash/hash-object 都可能加剧），交由 codex 恢复
- 已产出：docs/git恢复后待办-2026-08-06.md（提交策略 + 遗留问题 handleFarmTap 守卫 + 探针 cardCount 选择器改进）
- 关键：相簿代码全部安全在工作区；da9a6e6（round2）提交完好
- 教训：多 AI 并发操作同一 git 仓库有风险；检测到对象损坏应立即停手报告，不要自行 gc/repack

