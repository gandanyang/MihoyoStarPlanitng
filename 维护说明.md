# 归星物语 — 维护说明文档

> 本文档面向日后接手维护的开发者，假设你对 Phaser / TypeScript 零基础也能看懂。

---

## 一、项目是什么

一个浏览器里跑的 2D 像素风小游戏 Demo。

融合了：
- **星露谷物语**的种田生活循环（锄地→播种→浇水→收获）
- **米哈游式二次元世界观**（星辰岛、星之碎片、神秘少女）

玩家在小岛上度过一天，体验：探索 4 个区域 → 种萝卜 → 和 NPC 对话 → 完成主线任务"星之碎片" → 睡觉过夜 → 作物成熟收获。

**0.1 版本只做核心循环，不含战斗/联机/商店/背包UI。**

---

## 二、技术栈（只需知道这几个词）

| 技术 | 是什么 | 在本项目的作用 |
|------|--------|--------------|
| **Phaser 3** | 2D 游戏引擎 | 渲染画面、物理碰撞、场景管理、输入处理 |
| **TypeScript** | 带类型的 JS | 所有游戏代码（.ts 文件） |
| **Vite** | 前端构建工具 | 开发热更新、打包 |
| **Tiled** | 地图编辑器 | 画地图（已用脚本自动生成，一般不用手动开） |

---

## 三、快速开始（3 步跑起来）

```powershell
# 1. 进入项目目录
cd c:\Users\Gdy\Documents\trae_projects\Stellaris-Requiem

# 2. 安装依赖（首次或拉取新代码后）
npm install

# 3. 启动开发服务器
npm run dev
```

浏览器打开 `http://localhost:5174/`（端口看终端输出，通常是 5173 或 5174）。

> **遇到缓存问题？** URL 后面加 `?v=任意字符` 强制刷新，如 `http://localhost:5174/?v=1`

---

## 四、项目文件结构（每个文件干什么）

```
Stellaris-Requiem/
├── src/                        ← 所有源代码
│   ├── main.ts                 ← 游戏入口：创建 Phaser 实例、注册 8 个场景、挂载 Debug API
│   ├── config.ts               ← 全局配置（画布大小、背景色、标题、isMobileLayout）
│   │
│   ├── scenes/                 ← 场景（Phaser 的"关卡"概念）
│   │   ├── TitleScene.ts       ← 标题画面（封面图 + Enter 开始）
│   │   ├── StationScene.ts     ← 车站开场（序章剧情 + 跳过按钮）
│   │   └── MapScene.ts         ← ★核心：通用地图场景，一个类承载 6 个区域
│   │
│   ├── entities/               ← 游戏实体
│   │   ├── Player.ts           ← 玩家：WASD 移动、朝向、碰撞体
│   │   └── NPC.ts              ← NPC：数据 + 插值移动逻辑 + 剧本对话
│   │
│   ├── data/                   ← 数据层（模块级单例，跨场景保留）
│   │   ├── FarmState.ts        ← 农田状态：土地/作物/种子/成长结算/树木
│   │   ├── FarmProgress.ts     ← 等级经验系统
│   │   ├── Economy.ts          ← 金币经济（价格表）
│   │   ├── Inventory.ts        ← 物品库存（18 种物品）
│   │   ├── TimeSystem.ts       ← ★时间系统：唯一时间来源，tick/nextDay
│   │   ├── Stamina.ts          ← 体力系统（挖矿/砍树消耗，睡觉恢复）
│   │   ├── MineState.ts        ← 矿洞矿脉定义与开采状态
│   │   └── exits.ts            ← 出口配置：地图连通拓扑 + 出生点
│   │
│   ├── systems/                ← 系统层（业务逻辑）
│   │   ├── SaveSystem.ts       ← 存档系统（v0.5 分组结构 + 版本迁移）
│   │   ├── StorySystem.ts      ← 剧情系统（序章 11 步状态机 + 第一章对话）
│   │   ├── QuestSystem.ts      ← 主线任务：星之碎片状态机
│   │   ├── DailyQuestSystem.ts ← 每日任务（20 模板，含挖矿/砍树引导，随机 4 个）
│   │   ├── NPCSystem.ts        ← NPC 管理：6 个 NPC + 日程判定 + 剧本
│   │   ├── AudioSystem.ts      ← 音效（Web Audio 程序合成）
│   │   ├── InputManager.ts     ← 键盘输入（解耦）
│   │   └── TouchControls.ts    ← 触屏控件（摇杆 + 交互按钮）
│   │
│   ├── ui/                     ← UI（DOM 覆盖层）
│   │   ├── ShopPanel.ts        ← 商店面板
│   │   ├── BackpackPanel.ts    ← 背包面板（B 键）
│   │   └── StoryDialogue.ts    ← 全屏剧情对话（打字机）
│   │
│   └── vite-env.d.ts           ← Vite 类型声明（不用管）
│
├── public/assets/              ← 静态资源
│   ├── maps/                   ← Tiled 地图 JSON（farm/town/forest/mine/gate/house）
│   ├── tiles/                  ← 各地图主题瓦片集
│   ├── sprites/                ← 角色和物品精灵图
│   └── images/                 ← 封面图等
│
├── tools/
│   ├── gen_map_assets.py       ← 地图生成脚本（生成 maps/ 和 tileset）
│   └── gen_sprite_assets.py    ← 角色精灵生成脚本
│
├── test-tutorial.mjs           ← E2E：新玩家完整流程（node test-tutorial.mjs）
├── test-stress-switch.mjs      ← E2E：切图/挖矿压力测试（node test-stress-switch.mjs）
├── test-woodcutting.mjs        ← E2E：砍树机制（node test-woodcutting.mjs）
├── test-ch1-story.mjs          ← E2E：第一章主线+观星夜+存档恢复（node test-ch1-story.mjs）
├── index.html                  ← 网页入口（挂载 Phaser 画布）
├── package.json                ← 依赖和脚本
├── tsconfig.json               ← TypeScript 配置
├── vite.config.ts              ← Vite 配置
├── AGENTS.md                   ← Alpha 阶段开发指南（AI 必读）
└── .gitignore                  ← Git 忽略规则
```

---

## 五、核心架构（最重要的部分）

### 5.1 场景系统

```
main.ts 注册 8 个场景：
  new TitleScene()             ← 标题画面（首个自动启动）
  new StationScene()           ← 车站开场（序章）
  new MapScene('gate')         ← 庄园大门（教程地图）
  new MapScene('farm')         ← 农场（开局在 station 走完后进入）
  new MapScene('town')
  new MapScene('forest')
  new MapScene('mine')
  new MapScene('house')
```

**MapScene 是通用类**，通过构造函数传入的 key 决定加载哪张地图。6 个区域共用同一套代码，不重复。

**场景切换方式**：出口触发时用 `this.scene.start(target, { spawn })`（ScenePlugin 方式，会自动停止当前场景）。切图前 `camera.fadeOut(250ms)`，进入后 `fadeIn(300ms)`，另有 1500ms 兜底强制切换，防止黑屏。

**Debug API**（`window.debug`）：
```
window.debug.nextDay()           推进到次日 06:00（体力/矿脉/每日任务刷新 + 存档）
window.debug.setTime(21, 50)     设置当前时间（刷新 NPC 日程）
window.debug.advanceStory()      推进剧情步骤
window.debug.setStoryStep('done') 直接设置剧情步骤（'done' = 教程完成）
window.debug.getStoryStep()      读取当前剧情步骤
```

### 5.2 数据层：模块级单例

**关键理解**：`data/` 和 `systems/` 下的文件，export 的变量/函数是**模块级单例**。场景切换时不会销毁，刷新页面才重置。

```
FarmState.ts    → let tiles = new Map(...)     ← 跨场景保留土地状态
Inventory.ts    → const inventory = {radish:0}  ← 跨场景保留萝卜数
TimeSystem.ts   → const time = {day:1,...}      ← 跨场景保留时间
QuestSystem.ts  → let questState = 'not_started'← 跨场景保留任务进度
```

### 5.3 时间系统（唯一时间来源）

```
TimeSystem 是天数的唯一来源（Alpha 测试修复了 M1 双源问题）

时间流逝规则：
  现实 1 分钟 = 游戏 2 小时
  一天 06:00 ~ 22:00（共 16 游戏小时 = 8 现实分钟）
  到 22:00 停止，等玩家睡觉

过夜流程：
  玩家走进木屋，在床铺上或床边（面向床）按 E 睡觉
    → MapScene.trySleep()
      → TimeSystem.nextDay()
        → time.day += 1          ← 天数只在这里加
        → FarmState.advanceDay(time.day)  ← 成长判定（不存天数）
        → 时间重置 06:00
```

### 5.4 E 键交互优先级

```
按 E 时（MapScene.tryInteract）依次检测：

0. 靠近 NPC（所有场景，距离<24px）→ 全屏剧情对话（dialogue 剧本）
   - 村长特殊：对话由 QuestSystem 决定，含接受/交付任务
0.5 夏雅（gate 教程场景，未完成教程时）→ 对话给庄园钥匙
1. 森林靠近星之碎片（accepted 状态）→ 采集
2. 农场靠近树木（有旧斧头，24px 内）→ 砍树（3 击/棵，每击 -5 体力）
3. 矿洞靠近矿脉（24px 内）→ 挖矿（-5~15 体力）
4. 屋内床铺（house 场景 Ground 层 gid 9：站在床上或床边面向床按 E）→ 睡觉过夜
5. 农场农田面前格子 → 锄地/播种/浇水/收获
```

> 剧情对话打开时 update() 会提前 return，出口检测/移动暂停；对话关闭后恢复（小镇第一章开场即是如此）。

### 5.5 农场状态流转

```
empty →(E锄地)→ tilled →(E播种)→ planted →(E浇水)→ watered
                                                      ↓
                                              nextDay 成长判定
                                                      ↓
                                                   grown
                                                      ↓
                                              (E收获)→ tilled（土地保留，可重种）
```

成长条件：`plantDay + 1 <= 当前天数 且 watered = true`（播种次日且已浇水）

### 5.6 NPC 日程

6 个 NPC（村长/商店老板/神秘少女/矿工老张/花匠小梅/冒险家阿飞）共用日程，按时间段在各场景移动：

| 时段 | 位置 | 坐标(像素) |
|------|------|-----------|
| 06:00-08:00 | 农场 | (56, 184) |
| 08:00-12:00 | 小镇 | (248, 168) |
| 12:00-18:00 | 森林 | (248, 168) |
| 18:00-22:00 | 农场 | (56, 184) |

NPC 不做寻路，直接向目标点插值移动。`setTime` 后会 `rebuildNPCs` 重建当前场景 NPC。

**对话是剧本驱动的**：每个 NPC 的 `dialogues: DialogueLine[]`（`NPCSystem.ts` 定义），靠近按 E 时由 `StoryDialogue` 全屏打字机播放。村长对话由 `QuestSystem.getElderDialogue()` 按任务状态返回。

### 5.7 主线任务状态机

```
not_started →(村长对话)→ accepted →(森林采集)→ collected →(村长对话)→ completed
```

- `accepted` 状态时森林出现紫色星之碎片采集点
- `completed` 后村长对话变为结局台词

---

## 六、Debug API（浏览器控制台用）

游戏挂载了 `window.__game`（Phaser 实例）和 `window.debug`（快捷方法见 §5.1）：

```js
// 推进到次日 06:00（触发成长 + NPC/矿脉/每日任务重置 + 存档）
window.debug.nextDay()

// 设置当前时间（小时, 分钟），会刷新 NPC 日程
window.debug.setTime(21, 50)

// 直接操作场景（高级）
const s = window.__game.scene.getScenes(true)[0]   // 当前激活场景
s.player.setPosition(100, 100)    // 传送到坐标（注意：会触发出口检测）
s.player.facing = 'down'          // 设置朝向
s.player.x = 240; s.player.y = 30 // 直接瞬移（可用来触发出口区域）
s.tryInteract()                   // 模拟按 E
s.npcList                         // 当前场景 NPC 列表
s.oreSprites                      // 矿洞场景矿脉精灵列表

// 存档操作（v0.5）
localStorage.getItem('return_star_save')   // 查看原始存档 JSON
localStorage.clear()                       // 清空所有存档/进度
```

> HUD（时间/金币/体力）是 DOM 元素，不在 Phaser 对象里；需要读取时查 `document.getElementById('hud-overlay')`。

---

## 七、地图与出口

```
连通拓扑（真实出口以 src/data/exits.ts 为准）：
  车站 → 大门(gate) → 农场 ⇄ 森林 ⇄ 矿洞
                    ⇅          ⤢
                   小镇 ⇄ 矿洞
                    ⤵
                   家中(house)
```

地图尺寸：farm 为 40×25（640×400），其余（gate/town/forest/mine/house）为 30×20（480×320），瓦片 16×16。

| 出口 | 触发区域(像素) | 目标 | 出生点(像素) |
|------|--------------|------|------------|
| 大门→农场 | (224,0) 48×48 | farm | (240,96) |
| 农场→森林 | (224,0) 48×48 | forest | (240,272) |
| 农场→小镇 | (592,144) 48×48 | town | (48,160) |
| 农场→家中 | (80,288) 48×48 | house | (112,160) |
| 森林→农场 | (224,288) 32×32 | farm | (240,96) |
| 森林→矿洞 | (448,144) 32×32 | mine | (48,160) |
| 小镇→农场 | (0,144) 32×32 | farm | (432,160) |
| 小镇→矿洞 | (224,0) 32×32 | mine | (240,272) |
| 矿洞→小镇 | (224,288) 32×32 | town | (240,48) |
| 矿洞→森林 | (0,144) 32×32 | forest | (432,160) |
| 家中→农场 | (144,224) 32×16 | farm | (112,160) |

> **重要**：出生点必须放在目标地图出口区域**之外**（至少 2 格），否则切图瞬间会再次触发反向出口，形成循环瞬移（历史 bug：农场↔森林 33ms 往返）。改出口时注意核对。

---

## 八、地图修改

地图由 `tools/gen_map_assets.py` 脚本生成，**不要用 Tiled 手动编辑后保存**（会破坏出口缺口）。

如需修改地图：
1. 编辑 `gen_map_assets.py` 中对应函数（`gen_farm`/`gen_town`/`gen_forest`/`gen_mine`）
2. 运行 `python tools/gen_map_assets.py`
3. 农场顶部 col 14-15 row 0 必须留空（森林出口缺口）

---

## 九、已知问题与待优化

### 已知但暂不影响功能
1. **config.timeScale 未使用** — TimeSystem 硬编码了时间系数，改时间流速要改 `TimeSystem.ts`（MS_TO_GAME_MIN）
2. **收获后 crop.fillColor 残留** — 作物已隐藏但填充色未清，不影响显示
3. **多处过时注释** — 部分模块注释停留在旧版本描述
4. **剧情对话与出口互斥** — 全屏对话打开时出口检测暂停（设计如此）；若玩家在进小镇 600ms 内就离开，会错过第一章开场对话（`ch1TownIntroDone` 已在 create() 置位，不重放）

### 后续可扩展方向（按 AGENTS.md 优先级）
- P1 体验：音效反馈增强、操作手感、新手引导打磨
- P1 体验：商店/背包 UI 美化
- P2 内容：商店老板买卖规则剧情、更多 NPC 剧情
- P2 内容：夏雅模型/立绘升级（现 `npc_xiya` 形象不符人设；需覆盖 gate 开门关 + farm 教程 + E1 清晨偶遇三处形象，登记于 需求文档 §13.2）
- P2 内容：季节 / 天气系统、NPC 好感度 / 送礼
- 规划中：钓鱼系统（v0.6）、烹饪系统（v0.7）

---

## 十、常见维护操作

### 加一个新 NPC
1. `NPCSystem.ts` 的 `npcs` 数组加一个 `new NPC(id, 名字, 贴图, dialogues, buildSchedule(id))`
   - 第 4 参 `dialogues: DialogueLine[]` 是全屏剧本（StoryDialogue 打字机播放），没有特殊剧情就写几句通用问候
   - 第 5 参是日程，参考 `buildSchedule()` 按时间段返回目标坐标
2. 贴图：主角色 NPC 用独立贴图文件（如 `npc_xiya.png`），路人可复用现有贴图（npc_elder/npc_girl/npc_merchant）
3. 有任务逻辑的 NPC（如村长）对话由 `QuestSystem.getElderDialogue()` 按任务状态返回，不要写死在 npcs 数组

### 加一个新作物
现有 4 种作物（萝卜/番茄/玉米/草莓），定义集中在 `FarmState.ts`：
1. `CropType` 联合类型加新类型，`CROP_DEFS` 加定义（名称/图标/种子/生长天数/售价），`CROP_TYPES` 决定播种可选顺序
2. `Inventory.ts` 的 `ITEM_DEFS` 加对应作物 + 种子条目（如 `strawberry` / `strawberry_seed`）
3. 经济价格表同步 `Economy.ts`（购买/出售价）
4. 农田视觉 `updateTileVisual` 加新作物的显示

### 加一个新区域
现有 6 个区域（gate/farm/town/forest/mine/house），`MapScene` 一个类承载全部，流程：
1. `tools/gen_map_assets.py` 加 `gen_xxx()` 函数，运行 `python tools/gen_map_assets.py` 生成地图 JSON + 瓦片
2. `src/data/exits.ts` 加出口（`MAP_EXITS`）+ 中文名（`MAP_NAMES`）
3. `main.ts` 场景数组加 `new MapScene('xxx')`（目前共注册 8 个场景）
4. 注意：新区域出生点必须放在出口触发区域之外（至少 2 格），否则切图瞬间反向触发（见 §七）

### 改时间流速
`TimeSystem.ts` 第 46 行 `MS_TO_GAME_MIN`，当前 0.002（现实1分=游戏2时），改大=更快。

### 改农田位置/大小
`FarmState.ts` 的 `FARM_AREA`（瓦片坐标），同时改 `gen_map_assets.py` 的 `G_SOIL` 填充保持一致。

### 存档恢复逻辑在哪（重要）
- **真正入口是 `StationScene.create()`**：有存档且 `saveData.story.storyStep !== 'station_intro'` → `apply()` + `scene.start(saveData.player.scene, spawn)` 跳转到存档场景。
- `MapScene.ts` 的 farm 分支（`hasSave() && !this.spawn`）只是兜底：当玩家以 farm 场景直接启动且没有传入 spawn 时才恢复。
- **教训（2026-08-01 修复）**：判断"教程是否已过车站"必须读**存档里的** `saveData.story.storyStep`，不要读模块级 `getStoryStep()` —— reload 后模块状态尚未恢复，`getStoryStep()` 永远是初始值 `'station_intro'`，会导致存档恢复被静默跳过，玩家每次都重开序章。

### 改观星点位置（Demo 结尾）
`MapScene.ts` 的 `STARGAZE_POS`（像素坐标，当前 504,232 = 农场右下空地）。注意避开树木（`FARM_TREE_POSITIONS`）和出口区域；观星剧情文本在 `StorySystem.ts` 的 `DEMO_ENDING_DIALOGUE`（含三选项，StoryDialogue 选项行），结算面板在 `src/ui/EndingPanel.ts`；收尾完成状态为 `storyStep = 'observatory_complete'`（不采用 demoEndingDone 字段）。

### 打包 Android APK（Capacitor）
本机已装好环境，出包命令（Windows PowerShell）：

```powershell
$env:JAVA_HOME = "C:\Java\jdk-21.0.12+8"
$env:Path = "C:\Java\jdk-21.0.12+8\bin;$env:Path"
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
$env:GRADLE_OPTS = "-Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7897 -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7897"

npm run build                                  # 先出 Web 产物
npx cap sync android                           # 同步到原生工程（首次需 npx cap add android）
cd android; .\gradlew.bat assembleDebug --no-daemon    # Debug 包
cd android; .\gradlew.bat assembleRelease --no-daemon   # Release 包（签名）
```

产物：
- Debug：`android/app/build/outputs/apk/debug/app-debug.apk`
- Release：`android/app/build/outputs/apk/release/app-release.apk`

要点：
- 需要 **JDK 21**（Capacitor 8 要求），JDK 17 只够 Gradle 引导
- Gradle 下载不走系统代理，必须用 `GRADLE_OPTS` 显式传代理（本机 `127.0.0.1:7897`）
- `android/local.properties` 是本机 sdk.dir，不入库
- **Release 签名**：keystore 在 `C:\Users\Gdy\.android\guixing-release.keystore`（alias `guixing`，**务必备份，丢失无法更新已装 App**）；签名配置 `android/keystore.properties`（含密码，不入库）。新机器需重建 keystore 或复制该文件 + properties
- **物理返回键**：Android WebView 内由 `src/systems/AndroidBackHandler.ts` 处理（关对话/面板 → 回退场景 → 退出），浏览器内不生效
- 详细记录见《APK打包可行性方案.md》§8

---

## 十一、Git 版本历史

```
c19fdb3 v0.5: 存档格式分组升级(player/world/farm/story) + 版本迁移清档 + 教程E2E重写
bf7a777 修复: 地图切换/挖矿黑屏防御 + 农场出口循环瞬移 + 第一章小镇剧情
807d3a1 v0.4: 序章剧情(归乡) + 车站场景 + 新手教程
8b4f6d5 v0.3: 每日任务系统 + 室内房屋 + 挖矿系统(初级版)
b39e344 美术: 替换玩家精灵为林澈像素角色(4方向x4帧), 更新四地图瓦片集
bd88d80 0.25 核心系统：存档持久化 + 音效 + 背包 + 经验HUD + 美术升级
cc8f1e9 0.2 部署：GitHub Actions Pages workflow
38e854b 0.2 收尾：README 更新 + 系统架构图 + 版本号升级
e0d479a 美术升级：角色16x16→32x32程序化像素 + Phaser适配
21285f4 Phase M4.1+M4.2：HUD信息分级+对话框底部适配
2b802d4 Phase M3 修复：触屏控件改为模块级单例，修复跨场景卡死
6bd9b0e Phase M3：虚拟摇杆+交互按钮（触屏控件）
324a805 Phase M2：画布自适应（Scale.FIT + 全屏防滚动）
ac942c3 Phase M1：输入架构重构（InputManager 中间层，解耦输入源）
ed76967 Phase 6 任务系统：主线星之碎片完整闭环，0.1版本完成
9fc4e12 修复 M1：天数统一由 TimeSystem 管理，消除双源风险
04fce16 Phase 5 NPC系统：3 NPC固定日程+插值移动+靠近对话
ce3ca73 Phase 4 时间系统：TimeSystem + HUD时间 + 睡觉过夜
79a3d6a Phase 3.5 收获系统：grown→tilled 收获闭环
8e3a120 初始提交（Phase 0-3.1）
```

远程仓库：`https://github.com/gandanyang/Stellaris-Requiem`
> SSH: `git@github.com:gandanyang/Stellaris-Requiem.git`
>
> **改名后首次 push 前，更新本地 remote URL：**
> ```powershell
> git remote set-url origin git@github.com:gandanyang/Stellaris-Requiem.git
> # 验证
> git remote -v
> git push --dry-run
> ```

---

## 十二、开发原则备忘

1. **优先完成可玩循环**，不追求美术质量
2. **每次只实现一个功能**，逐步验收
3. **不过度工程化**，不为"方便"创建重复系统（已有 `Inventory.ts` 就不要建 `NewInventory.ts`）
4. **模块级单例跨场景保留**，刷新页面重置
5. **TimeSystem 是唯一时间来源**，不要在别处自存天数
6. **地图用脚本生成**，不用 Tiled 手动保存
7. **代码注释用中文**

**Alpha 阶段铁律（详见 AGENTS.md）**：
- 稳定 > 新功能：修 bug / 防黑屏 / 防存档损坏 / 保教程稳定优先
- 体验 > 系统数量：完成一个闭环 > 添加十个半成品
- 禁止：新增战斗/抽卡/大地图/复杂养成/后端/替换 Phaser/大规模重构
- 架构问题：先提出方案，不直接重构
- 每次修改：先读文件 → 理解数据流 → 找最小修改点
- 修改必须验证：tsc + vite build + 教程/压力 E2E
