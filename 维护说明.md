# 星露谷二游版本 0.1 — 维护说明文档

> 本文档面向日后接手维护的开发者，假设你对 Phaser / TypeScript 零基础也能看懂。

---

## 一、项目是什么

一个浏览器里跑的 2D 像素风小游戏 Demo。

融合了：
- **星露谷物语**的种田生活循环（锄地→播种→浇水→收获）
- **米哈游式二次元世界观**（星辰岛、星之碎片、神秘少女）

玩家在小岛上度过一天，体验：探索 4 个区域 → 种萝卜 → 和 NPC 对话 → 完成主线任务"星之碎片" → 睡觉过夜 → 作物成熟收获。

**0.1 版本只做核心循环，不含战斗/联机/商店/背包UI。**

---

## 二、技术栈（只需知道这几个词）

| 技术 | 是什么 | 在本项目的作用 |
|------|--------|--------------|
| **Phaser 3** | 2D 游戏引擎 | 渲染画面、物理碰撞、场景管理、输入处理 |
| **TypeScript** | 带类型的 JS | 所有游戏代码（.ts 文件） |
| **Vite** | 前端构建工具 | 开发热更新、打包 |
| **Tiled** | 地图编辑器 | 画地图（已用脚本自动生成，一般不用手动开） |

---

## 三、快速开始（3 步跑起来）

```powershell
# 1. 进入项目目录
cd c:\Users\Gdy\Documents\trae_projects\mihoyoStarPlanting

# 2. 安装依赖（首次或拉取新代码后）
npm install

# 3. 启动开发服务器
npm run dev
```

浏览器打开 `http://localhost:5174/`（端口看终端输出，通常是 5173 或 5174）。

> **遇到缓存问题？** URL 后面加 `?v=任意字符` 强制刷新，如 `http://localhost:5174/?v=1`

---

## 四、项目文件结构（每个文件干什么）

```
mihoyoStarPlanting/
├── src/                        ← 所有源代码
│   ├── main.ts                 ← 游戏入口：创建 Phaser 实例、注册场景、挂载 Debug API
│   ├── config.ts               ← 全局配置（画布大小、背景色、标题）
│   │
│   ├── scenes/                 ← 场景（Phaser 的"关卡"概念）
│   │   ├── BootScene.ts        ← 启动场景（显示标题，自动跳转农场）
│   │   └── MapScene.ts         ← ★核心：通用地图场景，一个类承载 4 个区域
│   │
│   ├── entities/               ← 游戏实体
│   │   ├── Player.ts           ← 玩家：WASD 移动、朝向、碰撞体
│   │   └── NPC.ts              ← NPC：数据 + 插值移动逻辑
│   │
│   ├── data/                   ← 数据层（模块级单例，跨场景保留）
│   │   ├── FarmState.ts        ← 农田状态：土地/作物/种子/成长结算
│   │   ├── Inventory.ts        ← 物品库存：萝卜计数
│   │   ├── TimeSystem.ts       ← ★时间系统：唯一时间来源，tick/nextDay
│   │   └── exits.ts            ← 出口配置：4 区域连通拓扑 + 出生点
│   │
│   ├── systems/                ← 系统层（业务逻辑）
│   │   ├── NPCSystem.ts        ← NPC 管理：3 个 NPC + 日程判定
│   │   └── QuestSystem.ts      ← 任务系统：主线"星之碎片"状态机
│   │
│   └── vite-env.d.ts           ← Vite 类型声明（不用管）
│
├── public/assets/              ← 静态资源
│   ├── maps/                   ← Tiled 地图 JSON（farm/town/forest/mine）
│   └── tiles/                  ← 占位瓦片集图片
│
├── tools/
│   └── gen_map_assets.py       ← 地图生成脚本（生成 maps/ 和 tileset）
│
├── index.html                  ← 网页入口（挂载 Phaser 画布）
├── package.json                ← 依赖和脚本
├── tsconfig.json               ← TypeScript 配置
├── vite.config.ts              ← Vite 配置
├── .npmrc                      ← npm 镜像源（国内加速）
└── .gitignore                  ← Git 忽略规则
```

---

## 五、核心架构（最重要的部分）

### 5.1 场景系统

```
main.ts 注册 4 个 MapScene 实例：
  new MapScene('farm')   ← 开局自动启动
  new MapScene('town')
  new MapScene('forest')
  new MapScene('mine')
```

**MapScene 是通用类**，通过构造函数传入的 key 决定加载哪张地图。4 个区域共用同一套代码，不重复。

### 5.2 数据层：模块级单例

**关键理解**：`data/` 和 `systems/` 下的文件，export 的变量/函数是**模块级单例**。场景切换时不会销毁，刷新页面才重置。

```
FarmState.ts    → let tiles = new Map(...)     ← 跨场景保留土地状态
Inventory.ts    → const inventory = {radish:0}  ← 跨场景保留萝卜数
TimeSystem.ts   → const time = {day:1,...}      ← 跨场景保留时间
QuestSystem.ts  → let questState = 'not_started'← 跨场景保留任务进度
```

### 5.3 时间系统（唯一时间来源）

```
TimeSystem 是天数的唯一来源（Alpha 测试修复了 M1 双源问题）

时间流逝规则：
  现实 1 分钟 = 游戏 2 小时
  一天 06:00 ~ 22:00（共 16 游戏小时 = 8 现实分钟）
  到 22:00 停止，等玩家睡觉

过夜流程：
  玩家在木屋旁按 E 睡觉
    → MapScene.trySleep()
      → TimeSystem.nextDay()
        → time.day += 1          ← 天数只在这里加
        → FarmState.advanceDay(time.day)  ← 成长判定（不存天数）
        → 时间重置 06:00
```

### 5.4 E 键交互优先级

```
按 E 时（MapScene.tryInteract）依次检测：

0. 靠近 NPC（所有场景，距离<24px）→ 显示对话
   - 村长特殊：对话由 QuestSystem 决定，含接受/交付任务
0.5 森林靠近星之碎片（accepted 状态）→ 采集
1. 农场睡觉点（木屋旁 col 2-4, row 13-14）→ 睡觉过夜
2. 农场农田面前格子 → 锄地/播种/浇水/收获
```

### 5.5 农场状态流转

```
empty →(E锄地)→ tilled →(E播种)→ planted →(E浇水)→ watered
                                                      ↓
                                              nextDay 成长判定
                                                      ↓
                                                   grown
                                                      ↓
                                              (E收获)→ tilled（土地保留，可重种）
```

成长条件：`plantDay + 1 <= 当前天数 且 watered = true`（播种次日且已浇水）

### 5.6 NPC 日程

3 个 NPC（村长/商店老板/神秘少女）共用日程：

| 时段 | 位置 | 坐标(像素) |
|------|------|-----------|
| 06:00-08:00 | 农场 | (56, 184) |
| 08:00-12:00 | 小镇 | (248, 168) |
| 12:00-18:00 | 森林 | (248, 168) |
| 18:00-22:00 | 农场 | (56, 184) |

NPC 不做寻路，直接向目标点插值移动。`setTime` 后会 `rebuildNPCs` 重建当前场景 NPC。

### 5.7 主线任务状态机

```
not_started →(村长对话)→ accepted →(森林采集)→ collected →(村长对话)→ completed
```

- `accepted` 状态时森林出现紫色星之碎片采集点
- `completed` 后村长对话变为结局台词

---

## 六、Debug API（浏览器控制台用）

游戏挂载了 `window.__game`（Phaser 实例）和 `window.debug`：

```js
// 推进到次日 06:00（触发成长 + NPC 重置）
window.debug.nextDay()

// 设置当前时间（小时, 分钟），会刷新 NPC 日程
window.debug.setTime(21, 50)

// 直接操作场景（高级）
const s = window.__game.scene.getScenes(true)[0]
s.player.setPosition(100, 100)    // 传送到坐标
s.player.facing = 'down'          // 设置朝向
s.tryInteract()                   // 模拟按 E
s.timeText.text                   // 读当前时间显示
s.hudText.text                    // 读主 HUD
s.questText.text                  // 读任务目标
s.tileRects.get('11,8')           // 读农田格子状态
s.npcList                         // 当前场景 NPC 列表
```

---

## 七、4 区域地图与出口

```
连通拓扑：
  森林 ──── 矿洞
    │        │
  农场 ──── 小镇

每张地图 30×20 瓦片，每瓦片 16×16 像素。
出口配置在 src/data/exits.ts。
```

| 出口 | 触发区域(像素) | 目标 | 出生点(像素) |
|------|--------------|------|------------|
| 农场→森林 | (224,0) 32×32 | forest | (240,272) |
| 农场→小镇 | (448,144) 32×32 | town | (48,160) |
| 森林→农场 | (224,288) 32×32 | farm | (240,48) |
| 森林→矿洞 | (448,144) 32×32 | mine | (48,160) |
| 小镇→农场 | (0,144) 32×32 | farm | (432,160) |
| 小镇→矿洞 | (224,0) 32×32 | mine | (240,272) |
| 矿洞→小镇 | (224,288) 32×32 | town | (240,48) |
| 矿洞→森林 | (0,144) 32×32 | forest | (432,160) |

---

## 八、地图修改

地图由 `tools/gen_map_assets.py` 脚本生成，**不要用 Tiled 手动编辑后保存**（会破坏出口缺口）。

如需修改地图：
1. 编辑 `gen_map_assets.py` 中对应函数（`gen_farm`/`gen_town`/`gen_forest`/`gen_mine`）
2. 运行 `python tools/gen_map_assets.py`
3. 农场顶部 col 14-15 row 0 必须留空（森林出口缺口）

---

## 九、已知问题与待优化

### 已知但暂不影响功能
1. **physics.debug 仍开启** — 画面显示绿色碰撞框，`main.ts` 第 22 行改 `false` 可关
2. **config.timeScale 未使用** — TimeSystem 硬编码了时间系数，改时间流速要改 `TimeSystem.ts`
3. **收获后 crop.fillColor 残留** — 作物已隐藏但填充色未清，不影响显示
4. **多处过时注释** — Player.ts 提到已废弃的 MainScene，FarmState 注释提到旧版本

### 后续可扩展方向
- 美术资源替换（当前全是占位方块/椭圆）
- 背包 UI / 商店系统
- 多作物种类（目前只有萝卜）
- 季节 / 天气系统
- NPC 好感度 / 送礼
- 更多剧情任务
- localStorage 存档（刷新页面目前会重置）

---

## 十、常见维护操作

### 加一个新 NPC
1. `NPCSystem.ts` 的 `npcs` 数组加一个 `new NPC(...)`
2. 给个独特 color 和 dialogue

### 加一个新作物
1. `FarmState.ts` 的 `CropData.cropType` 联合类型加新类型
2. `MapScene.tryFarmInteract` 播种时根据选中的种子设置 cropType
3. `updateTileVisual` 加新作物的视觉

### 加一个新区域
1. `gen_map_assets.py` 加 `gen_xxx()` 函数，运行生成地图 JSON
2. `exits.ts` 加新区域出口配置
3. `main.ts` 场景数组加 `new MapScene('xxx')`
4. `MAP_NAMES` 加中文名

### 改时间流速
`TimeSystem.ts` 第 46 行 `MS_TO_GAME_MIN`，当前 0.002（现实1分=游戏2时），改大=更快。

### 改农田位置/大小
`FarmState.ts` 的 `FARM_AREA`（瓦片坐标），同时改 `gen_map_assets.py` 的 `G_SOIL` 填充保持一致。

---

## 十一、Git 版本历史

```
ed76967 Phase 6 任务系统（0.1 完成）
9fc4e12 修复 M1：天数统一由 TimeSystem 管理
04fce16 Phase 5 NPC系统：3 NPC固定日程+插值移动+靠近对话
ce3ca73 Phase 4 时间系统：TimeSystem + HUD时间 + 睡觉过夜
79a3d6a Phase 3.5 收获系统：grown→tilled 收获闭环
e823afa Phase 3.4 时间与成长接口
b096998 Phase 3.3 浇水系统
5ac5087 Phase 3.2 种子系统
8e3a120 初始提交（Phase 0-3.1）
```

远程仓库：`https://github.com/gandanyang/MihoyoStarPlanitng`

---

## 十二、开发原则备忘

1. **优先完成可玩循环**，不追求美术质量
2. **每次只实现一个功能**，逐步验收
3. **不过度工程化**，0.1 不做背包/商店/复杂系统
4. **模块级单例跨场景保留**，刷新页面重置
5. **TimeSystem 是唯一时间来源**，不要在别处自存天数
6. **地图用脚本生成**，不用 Tiled 手动保存
7. **代码注释用中文**
