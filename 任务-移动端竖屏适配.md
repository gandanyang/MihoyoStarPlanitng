# 任务卡：移动端竖屏表现适配（P1 体验）

> 指派对象：待定（实现 agent）
> 发起人：opencode（实测探针发现）
> 优先级：P1（体验打磨——竖屏下画布过小 + DOM UI 错位）
> 状态：**方案已拍板（2026-08-02 制作人决策：方案 A），待实现指派**

---

## 1. 问题描述（2026-08-02 实测数据）

移动端竖屏（iPhone 375×812 视口）下实测：

```
游戏画布实际显示：375×281（Scale.FIT 缩放 800×600）
画布居中于 y=265，上方黑边 265px + 下方黑边 266px（黑边占屏高 65%）
```

**问题 A — 画布过小**：所有 32×32 贴图 → 16×16 显示 → 画布上每格仅 ~7px，
树/角色/场景细节在竖屏几乎不可辨，美术资源被浪费。

**问题 B — DOM UI 与画布错位**：
- 对话对话框实测在 y=672，画布在 y=265~546，**两者完全不重叠**
- HUD overlay（时间/经验条/任务目标）挂在 `game-container` 顶部 `top:0`，
  而画布居中在 y=265
- 触屏摇杆/按钮用 `position:fixed` 固定在视口角落，与画布分离

**根因**：`index.html` `body { display:flex; align-items:center }` + `main.ts`
`Scale.FIT` + `autoCenter` 竖屏时画布居中，但 DOM UI（HUD/对话/面板）用
absolute/fixed 相对视口定位，未跟随画布坐标。

## 2. 影响范围

- 移动端竖屏下：游戏画面偏小、对话/UI 悬在黑边区，体验割裂
- 不影响横屏（横屏画布占满，DOM UI 与画布重叠正常）
- 不影响桌面端（1024×768 宽屏，FIT 后无黑边问题）

## 3. 边界与禁止事项

- ✅ 允许：`index.html` / `main.ts` scale 配置最小改动；HUD/对话容器定位微调
- ✅ 允许：仅影响移动端竖屏的分支逻辑（`isMobileLayout()` 已有统一入口）
- ❌ 禁止：改动内部分辨率（800×600 世界坐标是系统基线，全场景依赖）
- ❌ 禁止：新增复杂横竖屏切换系统
- ❌ 禁止：改动游戏内美术贴图（本任务不涉及资源本身）
- ❌ 禁止：改动存档/剧情/玩法系统

## 4. 候选方案（实现时选一，均不改内部分辨率）

> **2026-08-02 制作人拍板：采用方案 A。** 理由：竖屏 375×281 画布 32×32→7px/格 实为"不可玩"而非"体验差"；方案 B 改动面覆盖 HUD/StoryDialogue/BackpackPanel/ShopPanel/EndingPanel/TouchControls 共 6 个 UI 系统，Alpha 稳定期风险不可接受；方案 C 不解决画布过小。手游横版竖屏强制横屏为行业标准。**实现方案 A 不得并行实现 B/C。**

**方案 A（已拍板，改动最小）**：移动端竖屏强制横屏提示
- `index.html` viewport 方向锁定或检测 `innerHeight > innerWidth` 时全屏提示
  "请旋转设备横屏游玩"，游戏在横屏下运行（当前横版玩法最适配）
- 优点：改动最小、彻底解决竖屏黑边+错位；缺点：不支持竖屏游玩
- 实现落点：`index.html` 增加 `screen.orientation.lock('landscape')` 尝试 + 竖屏检测时在 `#game-container` 上覆盖全屏提示层（纯 DOM，不进 Phaser），旋转后自动消失；`isMobileLayout()` 判断保持不变

**方案 B**：竖屏下 DOM UI 跟随画布
- HUD/对话容器改为相对 `game-container` 居中坐标计算（读取 canvas
  `getBoundingClientRect` 动态定位），对话/面板垂直居中于画布而非视口
- 优点：竖屏可玩；缺点：改动面大（HUD/StoryDialogue/BackpackPanel/ShopPanel/
  EndingPanel/TouchControls 全要跟随），风险高

**方案 C**：竖屏下保持黑边但统一底色 + HUD 居中于画布
- 黑边改为与游戏背景一致（非纯黑 `#111`），HUD 重算到画布顶部
- 优点：观感统一；缺点：仍不能解决画布过小的问题

## 5. 验收标准（DoD）

- [ ] 竖屏（375×812）下不再出现"对话/UI 与游戏画面割裂"（方案 A：横屏提示生效；
      方案 B/C：DOM UI 与画布坐标对齐）
- [ ] 横屏与桌面端表现不回归
- [ ] `npx tsc --noEmit` 通过；既有 E2E（tutorial/stress/ch1）不回归
- [ ] 实测数据更新：竖屏画布占用率、DOM UI 与画布重叠检查

## 6. 完成后提交

- 单独一个 commit，信息建议：`fix: 移动端竖屏表现适配（画布/DOM UI 对齐）`
- 提交前 `git status` 确认只包含本任务相关文件

## 7. 备注（实测方法，复现用）

```bash
# 用 puppeteer 以 375×812 isMobile 视口打开游戏：
#   - 画布 rect = { y: 265, h: 281 }
#   - 对话对话框 rect = { y: 672, h: 120 }（与画布不重叠）
#   - HUD overlay top:0 相对 game-container（全屏）
```
截图留存：`test-screenshots/mob-*.png`
