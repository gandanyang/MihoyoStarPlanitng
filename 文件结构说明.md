# 项目文件结构说明

> 本文档说明《归星物语》项目的文件组织规则：什么文件放在哪里、为什么这样放。
> 维护原则：根目录只放高频查阅的"项目宪法"和构建配置，其余归档到子目录。

---

## 一、整体目录树

```
mihoyoStarPlanting/
├── src/                    # 游戏源码（TypeScript + Phaser）
├── public/                 # 静态资源（游戏运行时加载）
├── tools/                  # 资源生成脚本（Python，确定性输出）
├── docs/                   # 文档归档（任务卡 / 报告）
├── tests/probes/           # 测试探针脚本（E2E / 回归）
├── android/                # Capacitor 安卓工程（APK 打包）
├── skills/                 # TRAE 技能定义
├── workflow/               # ComfyUI 工作流配置
├── .github/workflows/      # CI/CD（GitHub Pages 部署）
├── 根目录 *.md             # 项目宪法 + 活跃追踪文档（见 §三）
└── 构建配置文件            # package.json / vite / tsconfig 等（见 §四）
```

---

## 二、各目录职责与放置规则

### `src/` — 游戏源码
| 子目录 | 放什么 | 规则 |
|--------|--------|------|
| `scenes/` | Phaser 场景类 | 一张地图一个 MapScene 实例（通过 mapKey 区分），不复制场景类 |
| `systems/` | 游戏系统 | 已有系统不创建重复（如 Inventory 在 data/，不建 NewInventory） |
| `data/` | 纯数据层 | 状态/数值定义，无 Phaser 依赖 |
| `entities/` | 实体类 | Player / NPC |
| `ui/` | UI 面板 | DOM 面板组件 + ui-theme.css |

**规则**：新增系统优先归入既有目录，不新建顶层模块。

### `public/` — 静态资源
| 子目录 | 放什么 | 规则 |
|--------|--------|------|
| `assets/sprites/` | 角色/物件 sprite（32×32） | v2 像素风，逐像素绘制 |
| `assets/icons/` | 物品图标（16×16） | 由 tools/gen_item_icons.py 生成 |
| `assets/portraits/` | 立绘（512×768 半身） | 由 tools/gen_portrait_final.py 生成 |
| `assets/tiles/` | 地图 tileset | 按地图名命名（farm_tileset.png 等） |
| `assets/maps/` | Tiled 地图 JSON | 按地图名命名 |
| `assets/images/` | 杂项图片 | title_bg.jpg 等 |

**规则**：地图专属资源按地图名前缀（gate_tileset / npc_xiya），不混用。

### `tools/` — 资源生成脚本
| 脚本 | 职责 |
|------|------|
| `gen_map_assets.py` | 地图 tileset 生成（固定 seed=42） |
| `gen_sprite_assets.py` | sprite 生成 |
| `gen_style_unify.py` | v2 像素风风格统一 |
| `gen_item_icons.py` | 物品图标生成 |
| `gen_portrait_final.py` | 立绘生成 |
| `gen_npc_extras.py` | NPC 扩展资源 |
| `check_*.py` | 地图校验脚本 |

**规则**：**不新建生成管线**，扩展既有脚本（[美术规范 §5](美术规范.md)）。脚本必须确定性（固定种子，可重复输出）。

### `docs/` — 文档归档
| 子目录 | 放什么 | 示例 |
|--------|--------|------|
| `docs/tasks/` | 任务卡（已验收或进行中的专项任务） | 任务-美术v0.6视觉升级.md |
| `docs/reports/` | 报告 / 试玩记录 / 测试方案 / 可行性方案 | iOS兼容性专项审查报告-v0.5.3.md |

**规则**：
- 任务卡命名：`任务-{主题}-{版本}.md`
- 报告命名：`{主题}报告-{版本}.md` 或 `{主题}方案.md`
- 已完成且无后续动作的任务卡仍保留（历史溯源），不删除
- 引用根目录文档用 `../../文件名.md`

### `tests/probes/` — 测试探针
| 类型 | 命名 | 用途 |
|------|------|------|
| `probe-*.mjs` | 探针（Puppeteer E2E） | 单点验证 / 回归测试 |
| `test-*.mjs` | 测试套件 | 模块功能测试 |

**规则**：
- 运行方式：`node tests/probes/<脚本名>.mjs`（需先 `npm run preview`）
- 探针无相对路径 import，可独立运行
- 临时调试探针用 `_tmp_` 前缀，完成后删除（不放 tests/probes/）

### `android/` — Capacitor 安卓工程
- 由 `npx cap add android` 生成，不手动改结构
- `build/` 产物已 gitignore
- 配置在根目录 `capacitor.config.ts`

### `skills/` / `workflow/` — 工具配置
- `skills/`：TRAE 技能定义（.skill 文件）
- `workflow/`：ComfyUI 工作流 JSON（立绘生成用）

---

## 三、根目录文档清单（项目宪法 + 活跃追踪）

以下文档**留在根目录**，因为高频查阅或作为 AI/开发者入口：

| 文档 | 性质 | 说明 |
|------|------|------|
| `AGENTS.md` | 开发规范 | AI 开发必读，优先级最高 |
| `TEST_RULES.md` | 测试规范 | 测试分级 Level 0-3 |
| `README.md` | 项目入口 | 项目简介 |
| `CHANGELOG.md` | 变更日志 | 每次发布更新 |
| `maid.md` | 维护助手 | AI 协作入口 |
| `顶层设计.md` | 项目宪法 | 制作人领地，最高设计权威 |
| `需求文档.md` | 需求登记 | 功能需求清单 |
| `开发规范.md` | 编码规范 | 代码风格 / 提交规范 |
| `美术规范.md` | 美术规范 | v2 像素风 / 资源管线 |
| `维护说明.md` | 维护手册 | 12 章维护文档 |
| `剧情.md` | 剧情设定 | 制作人领地 |
| `剧情开发规划.md` | 剧情规划 | 制作人领地 |
| `林澈圣经.md` | 角色圣经 | 制作人领地（主角设定） |
| `问题追踪.md` | 活跃追踪 | BUG 登记 + 观察项（高频更新） |
| `v0.6版本计划.md` | 活跃规划 | 下一版本计划 |
| `移动端UI优化规划.md` | 活跃规划 | 移动端迭代规划 |
| `安卓体验测试反馈收集.md` | 测试反馈 | 面向测试用户的反馈表 |

**规则**：
- 制作人领地文档（顶层设计 / 剧情 / 剧情开发规划 / 林澈圣经）不自行修改
- 活跃追踪文档（问题追踪）随开发进展持续更新
- 已完成的版本规划留在根目录直到版本结束，之后可归档到 docs/reports/

---

## 四、根目录构建配置文件

| 文件 | 用途 |
|------|------|
| `package.json` / `package-lock.json` | npm 依赖 |
| `vite.config.ts` | Vite 构建配置（host:true 供手机访问） |
| `tsconfig.json` | TypeScript 配置 |
| `capacitor.config.ts` | Capacitor 安卓打包配置 |
| `index.html` | Vite 入口 HTML |
| `.gitignore` | Git 忽略规则 |
| `.npmrc` | npm 配置 |

**规则**：这些文件必须放根目录，不可移动。

---

## 五、新增文件放置决策流程

新建文件时，按以下顺序判断放哪里：

```
1. 是构建配置 / 入口文件？ → 根目录
2. 是游戏源码？ → src/ 对应子目录
3. 是静态资源？ → public/assets/ 对应子目录
4. 是资源生成脚本？ → tools/（扩展既有，不新建）
5. 是测试探针？ → tests/probes/
6. 是任务卡？ → docs/tasks/
7. 是报告 / 试玩记录 / 方案？ → docs/reports/
8. 是项目宪法 / 活跃追踪？ → 根目录（见 §三）
9. 是临时调试文件？ → 用 _tmp_ 前缀放根目录，完成后删除
```

---

## 六、禁止事项

- ❌ 不要在根目录新建任务卡 / 报告（归档到 docs/）
- ❌ 不要在根目录新建测试脚本（放 tests/probes/）
- ❌ 不要新建生成管线（扩展 tools/ 既有脚本）
- ❌ 不要创建重复系统（如已有 Inventory 不建 NewInventory）
- ❌ 不要把临时文件留在根目录不删除
- ❌ 不要自行修改制作人领地文档（顶层设计 / 剧情 / 林澈圣经）

---

## 七、维护

- 本文档在目录结构调整后同步更新
- 新增子目录时在此登记职责说明
- 变更记录：

| 日期 | 变更 |
|------|------|
| 2026-08-02 | 初版：整理根目录后创建，记录 docs/tasks/ docs/reports/ tests/probes/ 归档规则 |
